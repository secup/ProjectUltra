# ProjectUltra — Modem Infrastructure Map (current valid infra)

**Last updated:** 2026-07-28 (BUG-BURST-STALE-GEOMETRY: 2 decoder stages, 1 knob, §7 4b/4c/7b, §8) · **Branch verified:** `main`

**Why this doc exists:** ~6 months of AI iteration left active code interleaved with scar
tissue, dead paths, and stale comments. Before any cleanup we need ONE place that says, per
stage, *what it is, where it lives (file:line), when it runs, and whether it is current valid
infra or removable.* This is that place. Built from a 4-way parallel code audit (RX chain /
FEC+framing / protocol+ARQ / TX+waveforms), file:line-verified, not written from memory.

> Maintenance: when you add/remove a stage or flip a knob's default, update the relevant row
> AND the §7 cleanup register. Keep classifications honest — a wrong 🟢/🔴 here misleads a
> future deletion.

## Classification legend
- 🟢 **ACTIVE** — on the production default path right now.
- 🟡 **EXPERIMENTAL** — env-gated, default OFF; in-flight / discovery scaffolding.
- 🟠 **LEGACY / FORCED-ONLY** — reachable only via a force flag or non-default config; not auto.
- 🔴 **DEAD / RESERVED** — unreachable in production (enum-only, test-tool-only, superseded).

---

## 1. RX signal chain  (raw audio → soft LLRs)

Producer/consumer split: `StreamingDecoder::feedAudio()` (audio thread, ring-buffer write only)
→ `processBuffer()` decode thread, 5-state machine `SEARCHING → SYNC_FOUND → DECODING`
(+`BURST_ACCUMULATING`, `MCDPSK_BURST_CONTINUING`). Note: `modem_rx_decode.cpp` is **not** DSP —
it's post-decode delivery/routing only.

| Stage | Purpose | Where | When | Class |
|------|---------|-------|------|-------|
| Ring-buffer write + overflow recovery | audio→buffer, force-reset on overflow | `streaming_decoder.cpp` (`feedAudio`) → `sync_controller_.ring_.writeSamplesToRingLocked`; **ring now owned by `sync::SyncRingBuffer` inside SyncController** (§7 C3) | every callback | 🟢 |
| Activity RMS gate | presence detector (HIGH 0.030/LOW 0.010) | `streaming_decoder.cpp:412` | every callback | 🟢 (instrumentation) |
| Tone-burst ACK monitor | reverse-channel FSK ACK detect (armed-only); scans the §15.5 SNR staircase durations `{12,25,50,100}` ms (2026-06-15 — was 25 ms-only, blind to the shorter SNR-adaptive ACK) | `streaming_decoder.cpp:404` | event-gated | 🟢 (§15 ACK) |
| RMS/signal-presence search gate | per-mode adaptive gate w/ noise-floor tracker | `sync_controller.cpp::acquireSearchWindow` (§7 C3: moved out of searchForSync) | SEARCHING hop (~100 ms) | 🟢 |
| Warm/light search-window planning | predict next-frame arrival, shrink LTS window | `sync_controller.cpp::acquireSearchWindow` → `::planWarmSearch` → `arrival_policy::planWarmSearchWindow` | connected light preamble | 🟢 core (warm-handoff now default; flag removed) |
| Connected light-LTS dispatch + §16.4 re-anchor | run detectDataSync, accept (pos-gate), arm full-chirp on reject streak | `sync_controller.cpp::detectConnectedLightSync` (§7 C3: moved from searchForSync) | connected data acq | 🟢 |
| Full-anchor light-LTS fallback | descriptor-chirp miss → control-threshold light DATA fallback | `sync_controller.cpp::detectFullAnchorFallback` (§7 C3) | connected re-anchor | 🟢 |
| **Cold dual-chirp sync (wideband)** | up+down chirp matched filter → timing+coarse CFO | `chirp_sync.hpp:353` (`detectDualChirp`) ← `ofdm_chirp_waveform.cpp:433` | cold acq / MC-DPSK handshake | 🟢 |
| Dual-listen narrowband chirp | 1250–1750 Hz chirp → `BandwidthMode::NARROW` | `streaming_sync_acquisition.cpp:824` | cold, disconnected | 🟢 (OFDM_NARROW entry) |
| Warm/light LTS data sync | LTS autocorrelation, no chirp, trusts `last_cfo_` | `ofdm_chirp_waveform.cpp:517` (`detectDataSync`) | connected data | 🟢 |
| CFO drift clamp + anti-replay | clamp per-frame CFO to established; dup-pos reject | `signal_policy::limitConnectedCFODrift` `streaming_sync_acquisition.cpp:930` | connected | 🟢 (INVARIANT: trust-but-clamp on fading) |
| Schmidl-Cox plateau `process()` state machine | S-C plateau→coarse CFO→LTS timing | `ofdm_stream_processor.cpp:243`; primitives `ofdm_sync.cpp:118/230/386` | — | 🟠 **forced-only** (OFDM_COX gone from auto; S-C *primitives* reused by warm-LTS, keep those) |
| toBaseband (NCO down-convert + CFO) | mixer + running `freq_correction_phase` | `channel_equalizer_baseband.cpp:97` | every symbol | 🟢 |
| extractSymbol (CP strip + FFT) | PocketFFT forward | `channel_equalizer_baseband.cpp:174` | every symbol | 🟢 |
| **LTS channel estimate** | per-carrier LS `H=rx/tx` over 2 LTS + guard-bin noise var | `channel_equalizer_lts.cpp:174` (`estimateChannelFromLTS`) | warm/cold preamble | 🟢 |
| Pilot LS update + α-smoothing | per-symbol pilot LS, α{1.0/0.5/0.1/0.9} | `channel_equalizer_pilot.cpp:513` | per data symbol | 🟢 |
| Pilot interpolation (de-sloped linear) | pilots→data carriers, phase-slope compensated | `channel_equalizer_pilot.cpp:1209` / `:945` | per symbol | 🟢 |
| Wiener/LMMSE 2-D interpolation | time-then-freq 1D LMMSE | `channel_equalizer_pilot.cpp:201` (`estimateWienerChannel`) | when scattered pilots negotiated | 🟢 (params env-overridable `ULTRA_WIENER_*`) |
| Decision-directed channel tracking | DD refine on flat carriers only (95% χ², 9:1 odds) | build `channel_equalizer_equalize.cpp:646`; merge `channel_equalizer_pilot.cpp:934` | QAM8/QAM16 flat | 🟢 (gate `last_fading_index<0.15`, BUG-8PSK-001). **`dd_qam16_*` naming = adaptivity "tell", rename candidate** |
| MMSE equalize (coherent) | `conj(H)·rx/(|H|²+σ²)` + per-carrier post-eq var | `channel_equalizer_equalize.cpp:444` | per symbol | 🟢 (coherent-only since thread A — the differential MMSE early-return was deleted; LMS/RLS branch gated by `config.adaptive_eq_enabled` → cold on default path) |
| DD per-carrier phase tracking | QPSK/BPSK phase corrections | `channel_equalizer_equalize.cpp:730` | coherent | 🟢 |
| Per-carrier adaptive LLR scaling | mag-EMA var tracking, fading carriers inflate noise | `ofdm_symbol_demap.cpp:303` | per symbol | 🟢 |
| Soft demap → LLRs | dispatch to `soft_demap::demap{…}` (coherent: BPSK/QPSK/QAM8–256) | `ofdm_symbol_demap.cpp:373` | per symbol | 🟢 (coherent-only since thread A — the differential DBPSK/DQPSK/D8PSK cases, the DD phase tracking, and the 2-pass D8PSK/DQPSK helpers were deleted) |
| Noise-variance estimate | guard-bin / `|H1−H2|²/4`; empirical floor off | `channel_equalizer_lts.cpp:662` | preamble + carried | 🟢 (`ULTRA_LLR_NOISE_EMP_FLOOR` diag, off) |
| **Doppler-coherence discriminator (Good/Moderate)** | per-frame `|H|²` snapshot (LTS mag²) → snapshot-lag-1 autocorr → coherence score (≥0.5 = Good); measures coherence TIME, which `fading_index` (fade DEPTH) cannot. `dopplerHz()` σ readout. | class `ofdm/doppler_coherence_estimator.hpp`; **hosted in `StreamingDecoder`** (member, reset per connection), fed in `streaming_sync_acquisition.cpp` `populateDecodeMetrics` (~per decoded frame); surfaced via decoder atomics → ModemEngine → `Connection::setChannelCoherence(score, doppler_hz, valid)` (the approximate `dopplerHz()` readout rides the same feed since 2026-07-03, consumed ONLY by the retx trough-pacing deferral — `docs/RETX_PACING_DESIGN_2026_07_03.md`); consumed gated in `connection_handlers.cpp` via `connection_policy::coherenceAdjustedFadingIndex` | per decoded OFDM frame; `valid()` after ≥24 snapshots | 🟢 (2026-06-16, UNCOMMITTED). Read-only + gated (raw `fading_index` until valid; invalid at CONNECT → zero default-path change). Hosted in the DECODER not the demod because burst transport rebuilds the OFDMDemodulator per group. GUI-proven good 0.70/moderate −0.11. BUG-DOPPLER-COHERENCE-MODECHANGE-WIPE (the `ULTRA_RATE_ADAPT` precondition) FIXED 2026-07-02: `Connection::setChannelCoherence` holds the last VALID verdict while CONNECTED (per-connection, cleared in `enterConnected`/`reset`), so a MODE_CHANGE/modem-layer rebuild can no longer blind the rate ladder; the mid-stream `requestModeChange` CW pick also routes through `coherenceAdjustedFadingIndex`. Design: `docs/CHANNEL_DISCRIMINATOR_DESIGN_2026_06_15.md` |
| **Burst group-start truncation guard** | never arm a burst group from a frame the `std::min(frame_len, available)` shortened (the requirement grew between the readiness check and the decode) — defer to SYNC_FOUND and retry | `streaming_ofdm_decode.cpp` `decodeCurrentFrame` (`frame_truncated`) | connected OFDM burst group start | 🟢 unconditional correctness guard |
| **Commanded-geometry resolver** | MISSED BURST_HEADER → broad commanded-rung reconstruction is default-OFF; a default-active narrow BI0 path restores Z81 only for an already wire-learned, exact same-rung 8PSK R2/3 cw4 or QPSK R3/4 cw3 profile, cadence-guarded against post-RTO ambiguity. Configured `N` is never completion authority: BI0 publishes a conservative sender-policy air-end ceiling in the arm transaction, searches under that ceiling, and finalizes only at a CRC-valid tail. BI1 fails closed because physical `N` is not independently recoverable before deinterleaving. | `streaming_ofdm_decode.cpp` `resolveCommandedGeometry`/`learnRungGeometry` + immediate air gate; `streaming_burst_interleave.cpp` unknown-N tail search/fail-close; mirror in `streaming_sync_acquisition.cpp::checkIfReadyToDecode`; snoop `modem_engine.cpp::transmitToneBurstAck` | connected OFDM burst group start, descriptor absent | 🟢 exact same-rung BI0 Z81 recovery; 🟡 broad `ULTRA_COMMANDED_GEOMETRY` default-OFF |
| MC-DPSK soft decode (Mode 1) | differential demod → LLRs → LDPC | `streaming_ofdm_decode.cpp:2290` (`decodeMCDPSKFrame`); demod core `multi_carrier_dpsk.hpp::demodulateSoft` | below-OFDM-floor/heavy fade | 🟢 (INVARIANT: `reset()`+`setCFO(frame.cfo_hz)` per frame). 2026-06-15: residual-carrier tracking added in `demodulateSoft` — (a) clock-offset+dial regression (per-carrier residual vs carrier freq), (b) decision-free M-th-power common-phase jitter tracker (coherence/activity/lock-gated). Config flag `MultiCarrierDPSKConfig::track_clock_offset` (default true); strict deadband no-op when idle (no-op on the shared-clock sim). Tolerates real soundcard ppm + slow carrier jitter. Also 2026-06-15: ratiometric guard at the `pre_ldpc_llr_reject` path (~`streaming_ofdm_decode.cpp:1166`) — a data-bearing (ratio not silent) MC-DPSK frame with weak 1-CW-peek LLRs falls through to the 4-CW wait gate instead of being mis-emitted as a PING (was PONGing a low-level CONNECT_ACK on real hardware). |

**CFO correction chain (the load-bearing invariant — 5 stages, all 🟢):**
1. Chirp coarse CFO — `chirp_sync.hpp:457` (gap-error). Coarse; ±2 Hz on fading.
2. Apply pre-correction + initial phase — `ofdm_chirp_waveform.cpp:915` → `setFrequencyOffsetWithPhase` → `toBaseband` NCO. INVARIANT: `processPresynced` must NOT reset the pre-set CFO/phase (`ofdm_stream_processor.cpp:824`).
3. LTS residual refine — `channel_equalizer_lts.cpp:310` (phase between 2 LTS symbols, ~0.3 Hz threshold).
4. Per-symbol pilot CPE tracking — `channel_equalizer_pilot.cpp:601` (±15° clamp).
5. **Feedback to cached `last_cfo_`** — `ofdm_chirp_waveform.cpp:997`. **INVARIANT: do not remove** — else wrong chirp CFO re-injects every frame → progressive drift → CW failures.

> Estimate-stage in-flight work (tasks #8/#9): `ULTRA_LTS_CFO_AVG` (lever ① CFO-clean 2-LTS
> averaging) and `ULTRA_LTS_DFT_DENOISE` (interim Gaussian smoother — being replaced by an
> active-band delay-domain LS denoise). Both gated, default OFF. See
> `docs/CHANNEL_ESTIMATE_REINFORCEMENT_DESIGN_2026_05_30.md`.

---

## 2. TX path

| Component | Where | When | Class |
|-----------|-------|------|-------|
| Constellation mapper (BPSK/QPSK/QAM8/16/32/64/256) | `modulator.cpp:85` (`mapBits`) | TX coherent | 🟢 QPSK/QAM8/QAM16; 🔴 QAM32/64/256 (no auto rung reaches them) |
| ~~Differential encoder (DBPSK/DQPSK/D8PSK)~~ | ~~`modulator.cpp:454`~~ | — | ❌ **REMOVED** thread A (commit `65b27b6`) — OFDM is coherent-only; the TX differential branches + `dbpsk_prev_symbols` are gone (the RX twin `lts_carrier_phases`/`lts_phase_offset` removed in `2f3c2ce`). MC-DPSK keeps its own differential modulator (`multi_carrier_dpsk.hpp`). |
| OFDM symbol builder (map→pilots→IFFT→CP) | `modulator.cpp:232` (`createOFDMSymbol`) | every TX symbol | 🟢 |
| `generateTrainingSymbols(count)` — **production LTS source** | `modulator.cpp:608` | preamble gen, always **count=2** | 🟢 |
| `generatePreamble()` — Schmidl-Cox STS×4+LTS×2 | `modulator.cpp:551` | — | 🔴 **DEAD** (test tools only; OFDM_COX legacy) |
| `createSchmidlCoxSTS()` | `modulator.cpp:332` | — | 🔴 DEAD (only caller is dead `generatePreamble`) |
| `generateProbe()` | `modulator.cpp:662` | — | 🔴 likely DEAD (no production caller — verify) |
| **Full chirp preamble** `[chirp][LTS×2]` | `ofdm_chirp_waveform.cpp:336` | PING/CONNECT, first frame, group-start, RESEND anchor | 🟢 |
| **Light data preamble** `[LTS×2]` | `ofdm_chirp_waveform.cpp:353` | connected DATA / burst members | 🟢 (saves ~1.2 s/frame) |
| Short re-anchor preamble `[short-chirp][LTS×2]` | `ofdm_chirp_waveform.cpp:364` | fading data re-anchor | 🟡 gated `adaptive_short_data_preamble_`; force-off when warm-handoff on |
| Per-burst LDPC Z push to demod | `ofdm_chirp_waveform.hpp:69` (`setActiveLDPCLiftingZ`) | burst encode | 🟢 (INVARIANT: must propagate Z to demod or deinterleaver gets 1296≠3888 bits → throws) |
| `encodeBurstLight()` — **the burst engine** | `streaming_encoder.cpp:429` | OFDM file/multi-frame burst | 🟢 (transport env-gated) |
| `encodeToneBurstAck()` — 4-FSK reverse ACK | `streaming_encoder.cpp:733` | reverse channel | 🟢 (§15; symbol duration SNR-adaptive via §15.5 staircase `symbolMsForSNR`, picked at `app.cpp` ACK callback off a lock-free in-band-SNR cache — 850→408 ms at ≥18 dB). **WIRE 2026-07-02 (twice):** (a) software-ALC `drive_advisory` (0 hold/1 up/2 down/3 rsvd) joined the CRC coverage; (b) wide-coherent-window lever: `frame_mask` widened 8→16 bits → payload 32→40 bits (uint64 container), fields shifted (advisory now [38..39], CRC msg 28 bits `kPayloadCrcMessageBits`), Hamming 3→4 blocks, burst 27→**34 symbols** (675→850 ms baseline) — **wire-breaking, lockstep builds only** |
| **Software-ALC RX level verdict** (per-burst data-RMS/chain-noise + crest factor → OK/LOW/CLIPPED) | `streaming_burst_interleave.cpp` `computeBurstLevelVerdict` (accum in `tryDemodulateNextBurstFrame`, reset at group start `streaming_ofdm_decode.cpp` burst-marker branch); policy `connection_policy.hpp` `alcLowHeadroomDb`/`alcClipCrestFactorDb`/`RxLevelVerdict`; feed `modem_protocol_binding.hpp` → `Connection::setRxLevelVerdict`; advisory stamped in the tone-burst SACK emit (`connection.cpp`); sender walk `app.cpp` `handleDriveAdvisory`/`effectiveTxDriveForContext` | every completed OFDM burst group | 🟢 (2026-07-02, BUG-QAM16-RIG-LEVEL-BUDGET lever; loop default-ON `ULTRA_SOFTWARE_ALC`, advisory log always-on; sim-no-op at reference levels, rig A/B pending) |
| PAPR reduction | `streaming_encoder.cpp:108` | post-modulate | 🟡 PARKED (skipped for all coherent mods → inert on data path) |

**Production OFDM data config** (`StreamingEncoder()` ctor `streaming_encoder.cpp:64`):
`fft_size=1024, num_carriers=59, sample_rate=48000, center_freq=1500, cp_mode=LONG,
modulation=DQPSK, code_rate=R1_4, use_pilots=true, pilot_spacing=10`.
⚠️ `pilot_spacing=10` is the **default only** — the waveform overwrites it per-rung via
`ofdm_link_adaptation::recommendedPilotSpacing(mod,rate)` (mod/rate-adaptive), synced back at
`streaming_encoder.cpp:218/903`.

**Burst-group INVARIANTS** (load-bearing, from inline comments):
- Group members use plain light LTS, **no per-frame chirp** — RX accumulates at fixed
  `burst_min_block_` stride; a chirp prefix misaligns FFT → 0/8 deinterleave (§14.25).
- BURST_HEADER descriptor declares group size / cw-per-frame / mod-rate / interleave flags /
  lifting Z so cross-station RX self-configures (fixed cross-station 0/8).

---

## 3. FEC + framing + interleaving

**LDPC (🟢).** 802.11n QC-LDPC, base matrices `src/fec/ldpc_802_11n.hpp:33` (+custom R1/4 `:89`),
`expand(rate, Zp)` `:165`. **N = Z·24**: Z=27→**648** (default), Z=81→1944. Min-sum decoder
`ldpc_decoder.cpp:312` (`decodeSoft`); **LLR convention: negative=bit 1, positive=bit 0**;
min-sum factor 0.75 default / **0.9375 on fixed-frame path** (`frame_v2.cpp:23` — two factors,
intentional). Max iters rate-dependent (`ldpc_codec.hpp:86`: R1/4→50 … R1/2→80). `setRate()`
skips rebuild when unchanged (hot-path, ~75 ms/rebuild on Pi 5).

Rate→K (per `getCodeParams`, `ldpc_encoder.cpp:39`): R1/4=162, R1/2=324, R2/3=432, R3/4=486,
**R5/6=540**, of 648. ⚠️ **Table duplicated 5×** (`ldpc_encoder.cpp:39`, `ldpc_decoder.cpp:25`,
`ldpc_codec.cpp:36`, `frame_v2.hpp:885`, `frame_v2.cpp:1872`) — consolidate.

| FEC/framing component | Where | Class |
|-----------------------|-------|-------|
| LDPC encoder / decoder | `ldpc_encoder.cpp:136` / `ldpc_decoder.cpp:312` | 🟢 |
| `LDPCCodec`/`ICodec`/`CodecFactory` wrapper | `ldpc_codec.*`, `codec_factory.*` | 🟢 thin/partly vestigial (RX calls `LDPCDecoder` directly for `setMinSumFactor`) |
| `CodecType::{LDPC_5G,CONVOLUTIONAL,TURBO,POLAR}` | `codec_factory.hpp:22` | 🔴 reserved enum stubs |
| SoftCombineBuffer (HARQ chase-combine) | `soft_combine.hpp:82` | 🟢 but **default-OFF** (`enabled_=false`; `Connection::setSoftCombiningHARQ`) |
| Fixed-CW data frame (4-CW default, 1–8) | `frame_v2.hpp:608`, `makeFixedDataFrame` `frame_v2.cpp:2535` | 🟢 |
| 1-CW control frame (20 bytes) | `frame_v2.hpp:439` | 🟢 |
| Code rate on wire — DataFrame flags (2-bit, R1/4–R3/4 only) | `frame_v2.hpp:272` (`RATE_MASK`) | 🟢 ⚠️ can't express R5/6 or R1/3 on this path |
| Code rate on wire — MODE_CHANGE / BURST_HEADER (full byte) | `frame_v2.hpp:537` / `:588` | 🟢 |
| BURST_HEADER descriptor (0x25): size/cw/mod/rate/interleave/Z | `makeBurstHeader` `frame_v2.cpp:450` | 🟢 default ON (`ULTRA_BURST_DESCRIPTOR`) |
| GROUP_ACK 0x26 / GROUP_NACK 0x27 (+fast-NACK §14.30) | `frame_v2.hpp:245` | 🟢 |
| DATA_START/CONT/END (0x31–33) | `frame_v2.hpp` | 🔴 scaffolding (file-segment types not fully used) |
| **FrameInterleaver** (intra-frame across CWs) | `frame_interleaver.*`; TX `frame_v2.cpp:1944` | 🟢 **always on for OFDM** (skipped when cw=1) |
| **ChannelInterleaver** (within-CW time×freq) | `include/ultra/fec.hpp:133`; `frame_v2.cpp:1914` | 🟢 **default ON** |
| **carrier_ldpc_interleaver** (air-grid carrier spread V1) | `carrier_ldpc_interleaver.*`; `ofdm_chirp_waveform.cpp:73` | 🟡 default-OFF, **auto-engages on masked carriers** (likely removal candidate) |
| **BurstInterleaver** (cross-frame N-group byte permute) | `burst_interleaver.*`; TX `streaming_encoder.cpp:530`, RX `streaming_burst_interleave.cpp:496` | 🟡 **DEFAULT-OFF for ALL mods** via `burstCrossFrameInterleaveOn(mod)` (2026-07-21, flipped from ≥16QAM-ON). Interleave couples time-localized deep fades across all codewords → whole-group craters; rig A/B MPG@20 (F440-449) interleave-OFF +37% goodput, 5/5 vs 4/5, 5× fewer full craters. Contradicts the 2026-06-14 Good@20 +47% (calmer epoch). Now per-frame SR-ARQ SACK everywhere. `ULTRA_BURST_INTERLEAVE=1` re-enables (A/B the Good@20 claim). Mechanism preserved, only the default flipped. |

**ARQ-profile INVARIANT** — `connection_policy::burstCrossFrameInterleaveOn()`
(`connection_policy.hpp:94`, default **false**) is the SINGLE source of truth deriving (a) TX
byte-permute, (b) TX ARQ semantics, (c) on-wire `BURST_FLAG_INTERLEAVE` bit. **OFF → per-frame
SR-ARQ masks; ON → whole-group ACK/NACK.** They disagreeing was the QAM16 offset-skip bug
(fixed d4b80b3) — do not re-introduce a second source.

---

## 4. Protocol / operating modes / ARQ

**Three modes:** MC-DPSK (`0x04`, handshake + low-SNR floor, DQPSK R1/4) 🟢 · OFDM_CHIRP
(`0x05`, primary throughput) 🟢 · OFDM_NARROW (`0x06`, 500 Hz/21-carrier) 🟢. `isOFDMMode()`
(`frame_v2.hpp:107`) = `OFDM_CHIRP||OFDM_NARROW`.

**Selection flow:** handshake in MC-DPSK → dual-listen sets BandwidthMode → SNR measured →
`Connection::negotiateMode` (`connection_handlers.cpp:840`) → `recommendDataMode`
(`connection.cpp:654`) → `capInitialOFDMRate` → `recommendCWCountForChannel` →
`enterConnected` (`connection.cpp:4468`) → `configureArqForCurrentDataMode` (`connection.cpp:4114`).

**ARQ — ONE subsystem (Transport Merge, 2026-06-06):**
- `SelectiveRepeatARQ arq_` (`connection.hpp`): the SINGLE data transport for ALL modes — MC-DPSK
  data, OFDM_NARROW data, OFDM-wideband file/message, and all control ACKs. Window per mode —
  MC-DPSK `mcDpskWindowSizeForTiming` (1–5), OFDM_NARROW **3** (hardcoded), OFDM wideband
  `ofdmWindowSizeForChannel` **8 default, up to 16** on near-AWGN ≥R1/2, with the unified file/message
  burst bounded per-key-down to an **8.6 s** airtime budget (`prepareUnifiedBurstWindow` →
  `burstAirtimeBudgetFrames`; `kMaxBurstAirtimeMs` 7000→**8600** on 2026-06-07 = **5 frames** at
  QPSK R2/3, derived from live per-frame airtime; env `ULTRA_MAX_BURST_AIRTIME_MS` overrides). A
  timeout-repair RESEND re-anchors with a **full chirp+LTS** (`force_full_preamble=true`, 2026-06-07)
  so a missed warm-handoff group re-acquires deterministically. 🟢
- **The OFDM file/message burst** = TX framing (`sendNextFileChunk`/`sendNextFragment` →
  `flushBurstBuffer` → `transmitFrameBatch` → `encodeBurstLight` + BURST_HEADER descriptor) + RX
  group assembly (`burst_transport_rx_` collector, default `true` → `onBurstGroupReceived` →
  `processArqFrame` → `endGroupReceiveAndAck`) that **`arq_` drives**. ACK is a **tone-burst**
  (`connection.cpp` `setEmitToneBurstSackCallback` + `onToneBurstAck`). 🟢
- **REMOVED 2026-06-06 (R1b):** the separate `BurstStopAndWaitController burst_transport_` group
  controller (own seq space, group stop-and-wait, GROUP_ACK/GROUP_NACK control frames,
  `formAndSendBurstGroup`/`SR`, `startBurstFileTransfer`, `burst_transport.hpp`). `use_burst_transport_`
  remains a bool = "burst framing on" (always true; gates Z/descriptor/rescue/rate — collapse is a
  follow-up). GROUP_ACK/GROUP_NACK frame types are now unhandled (no emitter).

**Warm-handoff (§16)** 🟢 PRODUCTION DEFAULT (the `ULTRA_S16_WARM_HANDOFF` flag was removed
2026-05-31; behavior is now unconditional) — light LTS at group-start, BURST_HEADER as anchor
(`streaming_encoder.cpp`); supersedes (and permanently disables) the §16.4 short re-anchor.

**Frame flow:** PING/PONG → CONNECT/CONNECT_ACK → MODE_CHANGE/ACK → DATA/ACK → DISCONNECT/ACK
(all handshake/control in MC-DPSK).

**DATA payload discriminator** (`PayloadType`, `file_transfer.hpp:17`, first byte of a DATA
payload; NOT part of `DataFrame::HEADER_SIZE`):
- `0x00 TEXT_MESSAGE` 🟠 LEGACY-RX-ONLY — bare text, no identity. Nothing emits it; the RX
  strip survives only so an old-format object would still decode.
- `0x01/0x02/0x03 FILE_START/FILE_DATA/FILE_BLOCK` 🟢 — consumed by `FileTransferController::processPayload`.
- `0x04 TEXT_MESSAGE_OBJECT` 🟢 (2026-08-05) — `{0x04, object_id, text…}`; the ONLY format
  `sendMessage`/`sendMessages` emit. The id is allocated per logical object at admission
  (`createOutboundMessageRecord`) and is CONSTANT across geometry re-grids, which is what makes
  a re-send idempotent. TX wrap: `outboundMessageWireBytes` (`connection.cpp`). RX dedup:
  `messageObjectAlreadyDelivered` / `noteMessageObjectDelivered` against
  `delivered_message_object_ids_` (depth 8), applied in `handleDataPayload`
  (`connection_handlers.cpp`) AFTER reassembly and BEFORE any application callback. Both the id
  counter and the delivered-set are per-session (cleared in `disconnect()` and `reset()`).
  Binary payloads (`sendBinary`, the TNC path) carry NO identity and are NOT re-gridded.

**Stale-SNR decision screen** 🟢 (2026-08-05, no env knob — `connection_policy.hpp`):
`kConnectSnrStaleSentinelDb` (−10.0) is the WIRE marker for "no measurement" and is correct
there (encodes to byte 0 → peer renders n/a). It must never reach a decision: it is a finite
float, so a numeric consumer reads it as a catastrophic channel.
`isStaleSnrSentinel()` + the pure selector `decisionSnrFromWire(wire, last_real)` screen it;
`Connection::decisionSnrDb()` delegates and falls back to `measured_snr_db_`. **Four** decision
sites ride it — `requestModeChange()` (`connection_handlers.cpp`), `connection.cpp:4272`,
`:4336`, and `tryDescriptorModeSwitch()`. `wireSnrDb()` remains WIRE-ONLY; anything new that
consumes it for a decision is a bug. Note `ULTRA_WIRE_SNR_FRESH` is **DEFAULT-ON** (its
"default OFF" comment was stale, corrected 2026-08-05), so the sentinel is production-reachable.

**Build provenance** 🟢 (2026-08-05, `cmake/UltraBuildInfo.cmake` + `ultra_build_info` target):
`build_info.hpp` is regenerated at BUILD time, not configure time — the configure-time version
silently reported the commit the tree was configured on. `kBuildTimeUtc` is HEAD's COMMIT date
(stable per commit, so `copy_if_different` keeps incremental rebuilds at 0 TUs). **`--version`
is now trustworthy; before 2026-08-05 it was not** — historical campaign logs that cite it for
binary provenance should be treated with suspicion, and `strings` on new log literals remains
the independent check.

**Mandatory-escape message re-grid** 🟢 (2026-08-05, no env knob — `applyDataMode`,
`connection.cpp`): a receiver-commanded / stuck-frame demotion re-fragments the in-flight
message object at the new capacity and re-admits it at the FRONT of `queued_payloads_` in
admission order, instead of destroying it (BUG-MESSAGE-LOST-ON-FORCED-DEMOTE). Bounded by
`kMaxMessageGeometryRegridAttempts` (2), then it fails terminally with an attributed status.
Gated on `arq_.moveEpochEnabled()` — the forced move-epoch bump is what makes the peer discard
the stale partial prefix, so without it the path still fails closed and disconnects as before.

---

## 5. Waveform registry

`IWaveform` (`waveform_interface.hpp:48`); `WaveformFactory::create` (`waveform_factory.cpp:10`);
auto modes `getAvailableModes` = {MC_DPSK, OFDM_CHIRP, OFDM_NARROW}.

| Class | getMode | Class |
|-------|---------|-------|
| MCDPSKWaveform | MC_DPSK | 🟢 (handshake + low-SNR; `supportsDataPreamble()=false` → always full preamble) |
| OFDMChirpWaveform | OFDM_CHIRP | 🟢 primary OFDM data |
| OFDMChirpWaveform (`presets::narrowbandOFDM`) | OFDM_NARROW | 🟢 |
| ToneBurstEncoder | — (not IWaveform) | 🟡 4-FSK reverse ACK |
| OFDM_COX / Schmidl-Cox | enum `0x00` **removed** | 🔴 enum gone; STS code lingers in `modulator.cpp` (test-only) |
| OTFS_EQ/OTFS_RAW (`0x01/0x02`), MFSK (`0x03`) | — | 🔴 reserved; factory returns nullptr+WARN; not in `ModeCapabilities::ALL` |
| SC-DPSK (Barker-13) | `src/psk/dpsk.hpp` | 🟠 not an IWaveform, not in factory (CLAUDE.md table lists it — stale) |

Stale factory heuristics `WaveformFactory::recommendMode/getMinSNR/getMaxThroughput`
(`waveform_factory.cpp:112`) — legacy SNR-only tables superseded by `waveform_selection.hpp` +
`connection_policy.hpp`.

---

## 6. Master ENV-knob inventory

Buckets per the env-knobs→runtime-derivation workstream: **[FEAT]** in-flight feature flag ·
**[ADAPT]** channel-adaptive param that should become code-derived · **[DIAG]** diagnostic/oracle
(strippable) · **[FORCE]** test/dev force override.

| Knob | Effect | Default | Key site | Bucket |
|------|--------|---------|----------|--------|
| ~~`ULTRA_BURST_TRANSPORT`~~ | **REMOVED 2026-06-02** — burst transport is now unconditional (THE OFDM file path); the `=0` opt-out was deleted, no env read remains. RX default `burst_transport_rx_` flipped `false→true`. | (gone) | (removed from `connection.cpp`/`app.cpp`/`modem_engine.hpp`) | — |
| `ULTRA_ADAPTIVE_RATE` | per-block decode-headroom quality-feedback computation (GUI "Adapt:" bars + diagnostics); `=0` also disables the rate-CHANGE path below (`adaptive_rate_enabled_` gate). (Map previously said OFF — stale; the code default has been ON since 2026-06-07 "visibility" change.) | **ON** (opt-out `=0`) | `connection.cpp` ctor (`ULTRA_ADAPTIVE_RATE` read) | FEAT |
| `ULTRA_BURST_INTERLEAVE` | force cross-frame interleave ON→whole-group ACK / OFF→SR masks (overrides the default). **2026-07-21: default flipped to OFF for ALL mods** (was ≥16QAM-ON) — rig A/B MPG@20 (F440-449) showed interleave couples time-localized fades → whole-group craters; OFF (+37% goodput, 5× fewer full craters) via per-frame SACK. `=1` re-enables (A/B the 2026-06-14 Good@20 +47% claim, now contradicted); `=0` = default | **default OFF** | `connection_policy.hpp` `burstCrossFrameInterleaveOn(Modulation)` | FEAT |
| `ULTRA_PARTIAL_SACK_DESCRIPTOR_REPAIR` | BI0-only exact-provenance partial repair: receiver marks a descriptor-proven synchronous group in reserved tone-ACK advisory=3; sender snapshots the latest physical round's exact serialized DATA identities before/after that accepted ACK and engages only for a strict current-round partial. TX keeps a robust full BURST_HEADER but removes the redundant second 1.2 s DATA chirp and uses the light N budget. Any crater, RTO, prior/stale ACK mutation, BI1, singleton, mode/CW/Z change, unproven geometry, or ALC command falls back to double/full anchoring. Advisory=3 is HOLD-compatible for old/off senders; not capability-negotiated and conflicts with software-ALC use of the same field. Exact-identity mechanism proof remains valid (`9/8` and `11/6` cumulative ARQ correctly resolved as physical `4/8` and `5/6`), but the completed order-balanced MPG@20 A/B is a wash: +1.468% physical, `p=0.8289`; log effect -0.119%. Across eight enabled arms, 61 engagements removed 73.2 s mechanically, yet Pair 9 lost an engaged light repair after a 1/8 partial and the otherwise void Pair 1 repeated it after 3/6. Preserve acquisition diversity or fail closed on severe partials before retesting. | **OFF** (exact `=1`; MEASURED-WASH) | `connection.cpp` `PartialSackRoundContext` / `onToneBurstAck` / `flushBurstBuffer`; `selective_repeat_arq.cpp` exact identity query; `app.cpp` anchor dispatch | FEAT (acquisition redesign required) |
| `ULTRA_BURST_GROUP_FRAMES` | burst group size, clamp [2,32] | code **6** (`kBurstInterleaveGroupFrames` `:64`, reconciled 16→6 on 2026-05-30 — mask-width-matched to the 6-bit SACK frame_mask; the old 16 was un-SR-addressable on the default interleave-OFF path) | `connection_policy.hpp:74` | FEAT |
| `ULTRA_MAX_BURST_AIRTIME_MS` | per-key-down burst airtime ceiling → group size (frame count DERIVED from live per-frame airtime), clamp [5000,12000] | code **8600** (= **5 frames** at QPSK R2/3; 7000→8600 codified 2026-06-07 after a 20-seed Good@16 sweep tied groups 5/6 on goodput, 5 wins on key-down/SACK margin) | `connection.cpp` `burstAirtimeBudgetFrames` | FEAT |
| `ULTRA_COHERENT_WINDOW` | 🟢 ACTIVE (**DEFAULT-ON 16 since 2026-07-03**) — wide coherent ARQ window: SR-ARQ in-flight window for **coherent** wideband OFDM rungs (QPSK/8PSK/16QAM via `isCoherentModulation`) at rate ≥ R2/3 (was `kWideOFDMWindowFrames` 8 — the window-16 predicate was a differential-only fossil). Differential DQPSK/D8PSK keep their own high-throughput predicate; rates < R2/3 keep 8. Enabled by the tone-burst SACK `frame_mask` 8→16-bit widen (**WIRE-BREAKING** — payload 32→40 bits, burst 27→34 symbols, 675→850 ms baseline ACK; lockstep builds) + `kToneBurstAckWindowCapFrames` 8→16. A/B 2026-07-03 (all CRC-clean): Good@20 +18/+57/+26% (seeds 42/43/7), AWGN@20 3370→**3520 record**. Env: unset→16, `=8` legacy window, `=0` disable; clamp [0,16], read ONCE. PA-duty airtime budget still mins the real burst size. | **16 (on)** | `connection_policy.hpp` `coherentOFDMWindowOverride()` + `ofdmWindowSize()`; cap `kToneBurstAckWindowCapFrames` | FEAT |
| `ULTRA_BELOW_WINDOW_FILE_SALVAGE` | 🟢 ACTIVE (**DEFAULT-ON since 2026-07-03 evening** — 9/9 rig field engagements positive, ~60 frames rescued, every touched transfer byte-exact; `=0` opts out; graduate-under-structural-epoch-fix note stands) — formerly 🟡 EXPERIMENTAL (interim mitigation, 2026-07-03, **BUG-ARQ-SEQ-COLLISION**, rig-validation-pending) — receiver-side below-window FILE salvage: in the SR-ARQ out-of-window drop path (`handleDataFrame` else-branch), a strictly-BELOW-window DATA frame (half-space test, never far-future) whose payload is `FILE_START`/`FILE_DATA` is handed up the normal delivery callback (→ `Connection::handleDataPayload` → `FileTransferController::processPayload`, offset-idempotent dedup + straddle-merge) BEFORE the unchanged out-of-window SACK. Defuses the rate-change-abort seq collision under one-way ACK loss (rig W16: sender rewound to a stale base, re-chunked different bytes under retired seqs → 648 B permanent hole, 16.5 KB ghost, receiver strand). NEVER salvages TEXT/other payloads (seq-deduped only — would duplicate). Interim mitigation; the structural move-epoch fix is now IMPLEMENTED knob-gated (`ULTRA_ARQ_MOVE_EPOCH`, default-OFF, rig-validation-pending — see that row + KNOWN_BUGS); salvage stays as belt-and-braces. Grep: `SALVAGE below-window FILE frame`. Unset/`=0` → byte-identical. Read ONCE (ctor). | **OFF** | `selective_repeat_arq.cpp` ctor (env read) + `handleDataFrame` out-of-window branch (`below_window_file_salvage_`) | FEAT (interim) |
| `ULTRA_ARQ_MOVE_EPOCH` | 🟡 EXPERIMENTAL (**STRUCTURAL fix for BUG-ARQ-SEQ-COLLISION, 2026-07-03, edits-only/UNVALIDATED, rig-validation-pending**) — move-epoch carried on the wire so a stale-era ACK can never retire re-gridded seqs and the receiver never destroys a re-gridded resend. Two per-direction 2-bit (mod-4) counters in `SelectiveRepeatARQ`: TX epoch bumps on the `setCodeRate` TX-abort that rewinds `tx_next_seq_` (the collision precondition; `setFixedFrameCodewords`/`abortPendingTx` abandon forward — no re-use, no bump); every DATA frame carries the epoch in flags bits 6-7 (formerly the never-implemented rate-in-flags bits) + `EPOCH_REBASE` (formerly the never-implemented ENCRYPTED bit) on frames created at the window base; ACKs echo it (SACK bitmap bits 16-17 — bits 24-31 must stay clear of the `decodeSackBitmap` legacy shim; tone-burst payload bits 40-41, former Hamming zero-pad, NOT CRC-covered by design, airtime unchanged at 34 symbols). Sender IGNORES epoch-mismatched ACKs (`stale-epoch ACK ignored`, stats `stale_epoch_acks_ignored`; also structurally kills the below-base zombie-TX wart). Receiver adopts a new era from the wire: discard rx slots + ack state, re-anchor `rx_base_seq_` ONLY to an `EPOCH_REBASE` frame; a rebase-less adoption enters an ACK-SILENT unanchored interregnum (FILE payloads salvaged, sender RTO brings the era base) — naive re-anchor-to-any-seq would fabricate cumulative ACKs for lost head frames. **WIRE/SEMANTICS-BREAKING when ON — both stations lockstep** (a knob-OFF peer echoes epoch 0 → its ACKs are ignored after the first abort). Unset/`=0` → byte-identical (all epoch bits 0, no gating). Read ONCE (ctor). Salvage (`ULTRA_BELOW_WINDOW_FILE_SALVAGE`) stays as belt-and-braces. | **OFF** | `selective_repeat_arq.cpp` ctor (env read), `setCodeRate` (bump), `stampMoveEpochFlags`, `handleMoveEpochOnData`, `handleAckFrame` (gate), `sendSack`/`onToneBurstAck` (echo); `frame_v2.hpp` `Flags::EPOCH_MASK`/`EPOCH_REBASE`; `tone_burst_constants.hpp` `kBitOffsetMoveEpoch` | FEAT (structural, unvalidated) |
| `ULTRA_DESCRIPTOR_MODE_SWITCH` | 🟡 EXPERIMENTAL (**2026-07-03, edits-only/UNVALIDATED — Phase 1 of docs/MODE_SWITCH_PIGGYBACK_DESIGN_2026_07_03.md**) — descriptor-committed rate/mod switch for the wideband-OFDM fade-riding ladder: at a CLEAN burst boundary the sender's ladder move commits LOCALLY (`commitLocalModeSwitch`: `applyDataMode` now, NO `mode_change_pending_`/retry/TX-freeze) and the next burst's `BURST_HEADER` descriptor (control-profile QPSK R1/4, already the RX demod's authoritative per-burst config) IS the announcement — deletes the MODE_CHANGE round-trip (2.4-4 s clean; 18.5 s/retry × 2-9 receptions in troughs). Mandatory §2.6-arm-3 mitigation: commit arms a one-shot full chirp+LTS anchor for the next group (consumed in `flushBurstBuffer` → `force_full_preamble`; encoder anchor-skip streak resets via the `warm_descriptor=false` path) and the RX demotes warm-handoff on a mode-hop descriptor (fresh |H| under the new pilot/carrier geometry). RX protocol follow-through: new decoder→`Connection::onDescriptorModeChange` notification (wired in `wireModemToProtocol` like the burst-group path) runs the RX-side `applyDataMode` (window/timers/chunk capacity) — NO MODE_CHANGE ACK fires; confirmation = the group's tone-burst ACK. Phase-1 scope: clean-boundary ladder sites only (`applyAdaptiveRateFeedback` ×4); ESCAPE (mid-window) drops stay on the legacy exchange until `ULTRA_ARQ_MOVE_EPOCH` rig-validates (§7 hard dependency); single-frame-tail/message moves fall back to MODE_CHANGE (a 1-frame burst carries no descriptor, `encodeBurstLight:476-489`). MODE_CHANGE machinery fully retained for connect-time/USER_REQUEST/MC-DPSK/OFDM_NARROW/deaf-peer. Grep: `DESC-SWITCH commit` (sender) / `DESC-SWITCH adopt` (receiver); stats `descriptor_mode_switches`. **SEMANTICS-BREAKING lockstep when ON** (same policy as move-epoch). Unset/`=0` → byte-identical. Read ONCE (Connection ctor + StreamingDecoder ctor). | **OFF** | `connection.cpp` ctor (env read), `tryDescriptorModeSwitch`/`commitLocalModeSwitch`/`onDescriptorModeChange`, `flushBurstBuffer` (anchor one-shot); `streaming_decoder.cpp` ctor (env read); `streaming_ofdm_decode.cpp` BURST_HEADER intercept (hop capture + warm-handoff demotion + callback); `modem_protocol_binding.hpp` (wiring) | FEAT (Phase 1, unvalidated) |
| `ULTRA_RATE_ADAPT` | ACT on the §14.43 closed-loop quality feedback (receiver LDPC headroom→`rate_hint`→sender RateController). The loop is always WIRED (drives the GUI Adapt bars + diagnostics); this knob gates the actual rate CHANGE. The auto code-rate ladder is `{R1_4,R1_2,R2_3,R3_4}` (**R5/6 RETIRED 2026-06-17**; R5_6 still a forcible probe). **DEFAULT-ON for connected wideband OFDM (`OFDM_CHIRP`) since 2026-07-02** — the fade-riding ladder: `RateController` `climb_streak` 3→2 (env `ULTRA_RATE_CLIMB_STREAK` [1..16]), EMA/ssthresh unchanged. Env unset → ON (wideband only); explicit `=1` → old any-OFDM semantics; `=0` opts out; `ULTRA_LOCK_RATE=1` still pins. MC-DPSK/OFDM_NARROW stay fixed-rate by default. Precondition BUG-DOPPLER-COHERENCE-MODECHANGE-WIPE cleared same change (Connection-layer coherence carry). | **ON** (wideband OFDM; opt-out `=0` / `ULTRA_LOCK_RATE=1`) | `connection.cpp` `rateAdaptationActive` / `applyAdaptiveRateFeedback`; `rate_controller.hpp` ladder | FEAT |
| `ULTRA_QAM16_CLIMB` | QPSK-R3/4 → QAM16-R2/3 CROSS-MODULATION climb (2026-06-17; **default-ON + fade-riding retune 2026-07-02**). When adaptive rate is active and pinned at QPSK R3/4 for `kQam16ClimbStreak` consecutive clean groups (quality ≥ `climb_above`; default **2** — was 4 — env `ULTRA_QAM16_CLIMB_STREAK` [1..64]), the SENDER issues `requestModeChange(QAM16,R2_3)` at a CLEAN send boundary (reuses the `file_send_window_busy` gate → no ARQ-seq renumber/desync). The clean-group streak doubles as a low-variance Good/Moderate proxy (a Moderate channel's fades reset it). On QAM16: HOLD, or asymmetric PROMPT demote → QPSK R3/4 (**IMMEDIATE on one sub-`drop_below` group, `kQam16DemoteBadStreak=1` — was 2** — or a NACK). The 06-17 **sticky** no-reclimb was REPLACED by a re-climb COOLDOWN (`ULTRA_QAM16_RECLIMB_COOLDOWN`, base **3 clean groups**, doubling per demote, cap ×4; escape-drop counts double): under fade-riding the QPSK↔QAM16 oscillation IS the mechanism (the rung SHOULD oscillate with the ~10-20 s Good fade cycle) — sticky assumed oscillation was pathology and forfeited every later crest. `maybeEscapeStuckFrame` still demotes QAM16→QPSK R3/4 on a stuck-frame crater. Per-move cost ≈ 1.2 s full re-anchor (`kWideOFDMFullAnchorExtraMs`) + control round-trip ≈ 2.4-3 s sim / ~4 s rig; cooldown+backoff bounds worst-case thrash overhead <10% of airtime. | **ON** (opt-out `=0`) | `connection.cpp` `applyAdaptiveRateFeedback` (climb/demote), `maybeEscapeStuckFrame` (escape demote), `noteQam16Demoted`; members `qam16_clean_streak_`/`bad_streak_`/`reclimb_cooldown_`/`demote_count_` | FEAT |
| `ULTRA_QAM16_RECLIMB_COOLDOWN` | base re-climb cooldown after a QAM16 demote, in CLEAN groups [0..64] (0 = none). Doubles per demote this connection (cap ×4) — ssthresh-family multiplicative backoff bounding QPSK↔QAM16 MODE_CHANGE thrash. | **3** | `connection.cpp` `qam16ReclimbCooldownBase` / `noteQam16Demoted` | FEAT |
| `ULTRA_QAM16_R34` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03) — 16QAM **R3/4 crest rung** (+12.5% raw over R2/3; R2/3 measured ceiling 3520 bps AWGN@20 wide-window → R3/4 projected ~3900). ON → (1) `maxValidatedCoherentRate(QAM16)` lifts R2/3→R3/4 and (2) `applyAdaptiveRateFeedback` adds a within-QAM16 walk: climb R2/3→R3/4 after `qam16ClimbStreak()` (default 2) consecutive clean groups at QAM16 R2/3 (parallel counter `qam16_r34_clean_streak_`; busy window → hold + re-assert at the clean boundary, same as the mod hop); demote R3/4→R2/3 IMMEDIATELY on one bad group / NACK (`kQam16DemoteBadStreak=1` semantics; stays on QAM16 — does NOT arm the QPSK re-climb cooldown); `maybeEscapeStuckFrame` still drops STRAIGHT to QPSK R3/4 from EITHER QAM16 rate. NEVER connect-time selectable — `{QAM16,R3/4}` stays `kRungDisabledDb` in BOTH ladders (pinned in `test_connection_policy`). Risk: 16QAM R3/4 measured damage-bound PRE-interleave (55-70% loss, fable_analysis/07); known caveat: the escape from R3/4 is a mod-only same-rate transition (setCodeRate rewind skipped — see `applyDataMode` comment). Unset/`=0` → byte-identical. Read ONCE (static). | **OFF** | `waveform_selection.hpp` `qam16R34Enabled()` / `maxValidatedCoherentRate`; `connection.cpp` `applyAdaptiveRateFeedback` (walk/demote), `maybeEscapeStuckFrame` | FEAT (A/B) |
| `ULTRA_STUCK_ESCAPE_RETX` | 🟡 EXPERIMENTAL (A/B knob) — stuck-frame ESCAPE-drop trigger depth [2..10] (2026-07-02 campaign): retx count at which `maybeEscapeStuckFrame` demotes (QAM16→QPSK R3/4, else one code-rate rung — the shared action body is now `executeEscapeDrop`, 2026-07-03 refactor, semantics verbatim). Phase-0 forensics: the default 5 costs ~84 s frozen-base blind re-blast per 16QAM collapse (sim g43 + live rig MPG@20); A/B at 2-3 tests earlier trough exit vs fleeing salvageable rungs. Unset → 5 (byte-identical). Read ONCE (static). | **5** | `connection.cpp` `stuckRetransmitEscape()` / `maybeEscapeStuckFrame` / `executeEscapeDrop` | FEAT (A/B) |
| `ULTRA_MODE_CHANGE_RETRY_MS` | pin the MODE_CHANGE retry timer [1000..60000] for A/B. DEFAULT (unset) = **ratiometric control-exchange round trip** (2026-07-03, user-directed): `2×dataTurnControlGuardMs + kCarrierSenseSackCoalesceMs` ≈ ~5 s wideband OFDM, scaling with the current waveform's anchor/control airtimes by construction (MC-DPSK control → longer timer). REPLACES the old `arq_.getAckTimeout()` borrow — the multi-frame BURST deadline (~18.5 s measured, rig W4, growing with the ARQ window; ~74 s of one 328 s transfer was MODE_CHANGE dead-air). `MODE_CHANGE_MAX_RETRIES` 2→4 (same worst-case dead time as one old retry, 4× ACK-loss survival). | **0 (ratiometric)** | `connection.cpp` `modeChangeRetryMs()`; `connection.hpp` `MODE_CHANGE_MAX_RETRIES` | FEAT |
| `ULTRA_RETX_TROUGH_PACING` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, `docs/RETX_PACING_DESIGN_2026_07_03.md` §1) — master switch for the **trough-aware resend deferral**: after a ZERO-progress resend round (no ARQ base advance AND no new SACK bit — identity-agnostic via `SelectiveRepeatARQ::lastAckProgressFrames()`, dedup-safe; an RTO batch is zero-progress by definition) the next resend round is held `T_defer(n)=clamp(frac·Tc·2^(n−1)−elapsed_since_tx_end, 0, T_cycle/2)`, Tc=0.423/f_D (`retxTroughDeferMs`, pure policy fn; abs cap 8000 ms). f_D = measured `DopplerCoherenceEstimator::dopplerHz()` riding the coherence feed (new plumb decoder→ModemEngine→binding→`setChannelCoherence(score,hz,valid)`), else the ITU-R design Doppler of the (coherence-adjusted) fading class (Good 4230/Moderate 846/Poor 423 ms). ONE hold state gates BOTH resend triggers: turn refill (`runDeferredArqRefill` re-latch guard `retx_pace_hold_ms_`) AND per-slot RTO (`deferPendingRetransmits` bumps pending slot timers) — else the RTO leaks around the hold. Partial-SACK rounds resend immediately (unchanged); any progress early-releases the hold. Scope: CONNECTED wideband `OFDM_CHIRP` unified path, file SENDING with in-flight bytes — MC-DPSK/OFDM_NARROW never see it. Holds arm only BETWEEN key-downs (never splits a burst; receiver group timer runs intra-group only). Greps: `TROUGH-PACING defer`, `pace-hold` (Adapt text). Unset/`=0` → byte-identical (no hold armed, no timer touched). Read ONCE (static). | **OFF** | `connection.cpp` `retxTroughPacingEnabled()` / `noteArqRoundOutcome` / `runDeferredArqRefill`; `selective_repeat_arq.cpp` `deferPendingRetransmits`; `connection_policy.hpp` `retxTroughDeferMs` | FEAT (A/B) |
| `ULTRA_TROUGH_DEFER_TC_FRAC` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03) — `frac` in the trough deferral above, clamp [0.25, 4.0]; out-of-range/garbage → 1.0. Only meaningful with `ULTRA_RETX_TROUGH_PACING=1`. Read ONCE (static). | **1.0** | `connection.cpp` `troughDeferTcFrac()`; consumed by `connection_policy.hpp` `retxTroughDeferMs` | FEAT (A/B) |
| `ULTRA_COLLAPSE_ESCAPE_ROUNDS` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, design §2) — **collapse-conditioned escape**: 0 (OFF) or [2..8] = N consecutive zero-progress rounds at the current rung (with ≥⌈burst_cap/2⌉ frames in flight) → escape-drop via the SAME action as the 5-retx backstop (`executeEscapeDrop`: QAM16 either-rate → straight QPSK R3/4 + `noteQam16Demoted(2)`; else one rung + ssthresh). Zero-DELIVERED evidence, not retry depth — g42-protective by construction (ANY delivered/SACKed frame resets the round counter, so a lone straggler amid deliveries can never trip it; the rejected `ULTRA_STUCK_ESCAPE_RETX=3` hair-trigger could, −28% on g42). RTO-only rounds count (a cratered 16QAM may emit NO ack at all). Polled from the CONNECTED tick beside `maybeEscapeStuckFrame`; the 5-retx backstop stays verbatim. Counter resets on progress, `applyDataMode` (new era), `enterConnected`/`reset`. Grep: `COLLAPSE-escape`. Unset/`=0` → byte-identical. Read ONCE (static). | **0 (OFF)** | `connection.cpp` `collapseEscapeRounds()` / `maybeCollapseEscape` / `executeEscapeDrop`; round feed `noteArqRoundOutcome` (`onToneBurstAck` + `transmitFrameBatch`) | FEAT (A/B) |
| `ULTRA_RATE_CLIMB_STREAK` | `RateController.climb_streak` override [1..16] (code-rate ladder climb reaction, groups of `ema≥climb_above` needed to step up). | **2** (2026-07-02, was 3) | `rate_controller.hpp` ctor | FEAT |
| `ULTRA_PROMOTE_EMA_CARRY` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, sim/rig A/B **PENDING**) — **promote EMA carry** in the code-rate `RateController`: ON → after a PROMOTE, `resetSmoothingAfterChange(promoted)` seeds the post-move EMA at `climb_above` (0.70) instead of the neutral midpoint (0.475), so the new rung starts climb-ELIGIBLE and only the `climb_streak` (2 clean groups AT the new rung) gates the next move; a DEMOTE still resets to the midpoint. ASYMMETRY RATIONALE: after a demote the channel just proved WORSE than the EMA believed (history = invalid evidence, midpoint correct); after a promote the clean-group streak that EARNED the move is real evidence — the midpoint reset double-charges each rung (~2 extra clean groups re-reaching `climb_above` before the streak even starts ≈ ~4 clean groups ≈ 30-45 s PER RUNG at α=0.4; rig W9-W11 2026-07-03: ~90-120 s of every ~240 s transfer below 16QAM in exactly this arithmetic, on moves already justified by clean groups). NO RUNAWAY: one bad group pulls 0.70→0.42 (α 0.4, eligibility gone instantly), ~3 bad groups cross `drop_below` as before. SCOPE: the code-rate ladder ONLY — ssthresh ceiling / `noteRungFailed` / reprobe machinery untouched (carry also applies to the reprobe climb, which its own credit gate still guards); the QAM16 mod-hop streaks (`qam16_clean_streak_` etc., `connection.cpp`) are a separate mechanism, untouched. Env can only ENABLE (ORs into `Config::promote_ema_carry`, so tests drive the field directly); unset/`=0` → byte-identical. Read ONCE (static-lambda in ctor). | **OFF** | `rate_controller.hpp` ctor env read / `Config::promote_ema_carry` / `resetSmoothingAfterChange(bool promoted)`; tests `test_rate_controller.cpp` `test_promote_carry_*` | FEAT (A/B) |
| `ULTRA_R23_BASIS` | pin the ENTRY rate to QPSK R2/3 (SNR ≥ 18) on any FADING channel (`fading_index ≥ kFadingAwgnMax=0.15`), decoupling entry from the coin-flipping Good/Moderate classifier. Keeps R3/4 on clean AWGN (a blanket pin cost ~11% there: OTASim AWGN@20 R3/4 2150 vs R2/3 1920). Entry-only; the climb path is not gated. `ULTRA_ENTRY_CAP_R34` (next row) carves a data-aided R3/4-entry exception out of this clamp. **(2026-07-14, BUG-MPG20-OVER-DEMOTE-R14: the Good/Moderate boundary `kFadingGoodMax` itself was re-derived from 0.65 to the ML midpoint of the cluster centers = 0.76 — 0.65 sat ON the Good center (0.62). This reduces the coin-flip that motivated this knob AND fixes the per-group RX-authority verdict, which had NO connect-time-style mitigation and pinned R1/4 mid-transfer on Good@20.)** | **ON** (2026-06-17, fading-gated) | `waveform_selection.hpp` `capInitialOFDMRate` + `kFadingGoodMax` (single-source class boundary) | FEAT |
| `ULTRA_ENTRY_CAP_R34` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, sim/rig A/B **PENDING**) — **data-aided R3/4 ENTRY** exception to the `ULTRA_R23_BASIS` R2/3 clamp above. ON → `capInitialOFDMRate` admits a QPSK **R3/4 entry** — never higher: `min(candidate, R3/4)`, the cap never raises the candidate, and the 16QAM hop stays adaptive-only (the branch is QPSK-gated and rate-only) — IFF ALL of: (1) the reading is **DATA-AIDED** (`Connection::rateSelectionSnrDataAided()` now plumbed to both entry cap sites; a training-snapshot reading keeps the R2/3 cap — it fade-crest over-reads); (2) fading `< kFadingGoodMax` (**Moderate-class keeps the R2/3 cap unconditionally** — saturation-bound entries are deliberately marginal); (3) selection SNR clears the ladder's own QPSK R3/4 anchor (`kCoherentLadder`, Good 20.0) by ≥ `kConnectSnrReadingSigmaDb` **3.15 dB** = the MEASURED per-reading sigma (#58 forensics, 12 connects at constant dial MPG@20 — CHANGELOG 2026-07-03 connect-SNR-pool entry), i.e. R3/4 stays the ladder pick even if this single reading over-read by 1σ. WHY: the R2/3 clamp guarded the CHIRP-snapshot over-read, but since #58 the entry reading is the conservative fade-AVERAGED data-aided estimate (EVM only adds error); rig entries read 12.9-19.3 then burn ~60-90 s climbing R2/3→R3/4→16QAM (~90-140 s below 16QAM per ~250 s transfer — the largest remaining goodput loss). Gate = pure `dataAidedEntryClearsR34`; boundary-tested through the `capInitialOFDMRateImpl` seam (knob passed explicitly — the env read is latch-once). Unset/`=0` → byte-identical. Read ONCE (static). | **OFF** | `waveform_selection.hpp` `entryCapR34Enabled()` / `kConnectSnrReadingSigmaDb` / `coherentLadderAnchorDb` / `dataAidedEntryClearsR34` / `capInitialOFDMRateImpl`; call sites `connection_handlers.cpp` handleConnect, `connection.cpp` acceptCall; tests `test_waveform_policy.cpp` `test_entry_cap_r34` | FEAT (A/B) |
| `ULTRA_BURST_DESCRIPTOR` | emit BURST_HEADER descriptor | **ON** (escape hatch) | `modem_engine.cpp:530` | FEAT |
| `ULTRA_BURST_HEADER_ONCE` | descriptor only on group 0 | OFF | `modem_engine.cpp:544` | FEAT |
| `ULTRA_SIM_PAPR_PENALTY` | SIM-FIDELITY: drive the OTASim TX through the hardware peak-normalization (`normalizeTxBurstForHardware`, peak→`settings_.tx_drive`) instead of RMS-to-reference, so high-PAPR coherent-OFDM data frames deliver only (tx_drive/PAPR) average power vs the fixed reference-sized noise → models the peak-limited-PA penalty (~10.26 dB for coherent QPSK) the RMS-norm otherwise hides. Models the PAPR component only, NOT the cheap-card anchor-to-data gap. | **OFF** (byte-identical when off) | `app.cpp` `doQueueRealTxSamples` (sim branch ~2960) | DIAG/ADAPT |
| `ULTRA_SIM_TX_PEAK` | override the `ULTRA_SIM_PAPR_PENALTY` peak target (default = `settings_.tx_drive`); dial low (e.g. 0.12) to reproduce a low RX operating level. Clamped to the hardware peak range. | unset (=tx_drive) | `app.cpp` sim TX | DIAG/FORCE |
| `ULTRA_BURST_RMS_DIAG` | log per-collected-burst-frame gate inputs (`next_rms`/`anchor_rms`/`noise_floor` + ratios) for kept AND erased frames — for calibrating/confirming the relative erasure gate (esp. the next instrumented IONOS run) | OFF | `streaming_burst_interleave.cpp` `tryDemodulateNextBurstFrame` | DIAG |
| `ULTRA_BURST_ERASURE_ABSOLUTE` | force the LEGACY fixed `0.015` burst erasure gate (A/B harness vs the operating-level-relative default) | OFF (default = relative gate) | `streaming_burst_interleave.cpp` erasure gate | FORCE |
| `ULTRA_RX_AGC` | RX operating-level AGC: SLOW, AMPLIFY-ONLY, deadband normalizer in `feedAudio` (ring path only; ACK monitor stays raw) that raises a SEVERELY-low operating level (>9.5 dB below ref) toward the modem reference so the absolute gates (erasure, sync floor, CCA) work regardless of gain staging. Init high → exact no-op at normal level (verified good@20: OFF=ON=1710 bps, 0 engage); engages on a low channel (+13.6 dB at level 0.037). SNR-safe (scales signal+noise together). Labeled prototype — default off until IONOS-proven; production-clean form would sync-gate the level estimate. | **OFF** | `streaming_decoder.cpp` `feedAudio` | FEAT (prototype) |
| `ULTRA_ROBUST_IDLE_PING` | LOW-SNR HANDSHAKE FLOOR fix (#70). A PING is a bare chirp with no data (`encodePing`→`generatePreamble`); the receiver IDs it by a LEVEL test (`data/training RMS ratio < kPingMaxDataToTrainingRMSRatio=0.5`, abs floor `kPingChirpLockMaxDataRMS=0.16`). At low SNR broadband noise floods the silent gap (ratio 0.68–0.88 > 0.5) so a real PING reads as a faded CONNECT → waits for a 4-CW frame that never comes → no PONG → never connects (sim floor never < ~15 dB Good, vs the chirp itself locking solid corr 0.6–0.75 to ~6 dB AWGN). When ON, a solid chirp-lock (corr≥floor, gap OK) emits the PING on chirp signature ALONE — only for frames that already tripped the pre-LDPC false-lock reject (low LLR), so a cleanly-decodable CONNECT (good LLR→CW0 magic peek) is never short-circuited. Gated on `bare_chirp_expected_` (App sets FALSE during CONNECTING) so a faded CONNECT_ACK isn't mis-PONGed (#27-safe on the initiator). **STAGE2 LANDED 2026-07-01:** the RESPONDER now also sets `bare_chirp_expected_`=FALSE on PONG-TX + arms a ~20 s expects-CONNECT window (`app.cpp` PONG callback + tick re-arm, `responder_connect_expected_until_ms_`), closing the responder-starvation hole (a faded CONNECT after our PONG is decoded, not re-PONGed). **HIGH-SNR-SAFE GATE + DEFAULT-ON 2026-07-01:** the ratio can't separate a flooded low-SNR PING from a strong high-SNR false-sync (both ratio ~1.0), so the emit now also requires `data_rms ≤ kPingChirpLockMaxDataRMS=0.16` — excluding the loud good@20 false-syncs (data_rms 0.28–0.33) while passing quiet low-SNR PINGs (0.04–0.09). PROMOTED default-ON (opt-out `=0`). PROVEN: good@20 sim PASS with the emit ACTIVE (churn 2/2, 1860 bps); rig **MPM@8 pure-default zero-env connects + delivers CRC-clean** (2/2). Closes BUG-HANDSHAKE-PING-FLOOR. **STAGE 1.5 LANDED 2026-07-07 — the noise-floor-RELATIVE gate (level-invariant):** `evaluatePingFrame` now takes the receiver's own idle-noise floor (`IdleNoiseSNREstimator.filtered_noise_rms`) plus an IN-BAND gap RMS (identical 101-tap 50–2950 FIR run over the gap window) and adds `gap_is_noise = inband_gap ≤ floor × kPingNoiseGapFactor(1.5)` as a payload-absence arm — a bare PING's gap reads ≈ the floor at EVERY level and SNR while a real payload rides sqrt(1+SNR_lin) above it (3.3× at 10 dB). BOTH sides must be in-band: raw-domain comparison compresses the discriminant to ~1.49× at good@10 (a real CONNECT was misclassified — caught in sim). The CW0-peek wait gate also honors `gap_is_noise` (kills the multi-second 4-CW wait per noise-flooded probe), and the sync-found path feeds the estimator the pre-chirp ambient prefix (`SyncResult.preamble_start_sample`, boot-only via `hasEstimate()`) so the floor reference exists on a first-pass probe. PROVEN: sim good@10 CONNECT+PASS out-of-box (was never-connect), good@20 PASS 0 spurious, `UltraTncSimAudio` (the #70 ctest reproducer, red since 2026-07-02) PASS with handshake 16 s. The absolute 0.16 floor remains only as the no-estimate fallback. | **ON** (opt-out `=0`; low-SNR connect floor now shipped) | `streaming_ofdm_decode.cpp` (~511 knob, evaluatePingDecision lambda ~522 in-band gap FIR, CW0-peek gate ~1614, pre-LDPC emit ~1395); `streaming_frame_policy.hpp` (`kPingNoiseGapFactor`, `gap_is_noise`); `streaming_sync_acquisition.cpp` (pre-chirp prefix feed); `bare_chirp_expected_` ← `app.cpp` (state handler + STAGE2 PONG/tick) | 🟢 ACTIVE |
| `ULTRA_CONNECT_RATIOMETRIC_SNR` | CONNECT-TIME RATIOMETRIC SNR fix (#74). The responder's connect-time rate decision consumes `IDLE_IN_BAND` SNR = `10·log10(kModemReferencePower/idle_noise)` — a noise-only meter that ASSUMES the RX signal sits at the sim reference `kModemReferenceInBandRms=0.3048`; on a real radio whose RX level differs it OVER-READS by the level deficit → too-aggressive rate (rig MPG@10: idle 13.1 → R1/2 stall). The level-invariant `MCDPSK_IN_BAND` training SNR (`updateTrainingSNREstimate`, `10·log10(signal/residual)`, both 50–2950 Hz, = `|H|²/noise_var`) already exists for the handshake preamble but `populateDecodeMetrics` only routes it when `connected_`. When ON, the `connected_` gate is relaxed so the ratiometric value drives the connect rate too (rig: connect flips 13.1→8.0, picks robust rung). ESTIMATOR PROVEN sound (`test_mcdpsk_snr_calibration`): AWGN tracks true within ~0.1–0.7 dB to 30 dB (no saturation); the GOOD-channel cap (~12 sim/~18.7 rig) is the physical FADING coherence limit over the ~170 ms training window, correctly reported as effective SNR — NOT a defect. PROMOTED to DEFAULT-ON 2026-07-01 after the multi-channel rig A/B: on the rig idle over-reads and mis-selects a STALLING mode on BOTH Good (MPG@10 → QPSK R1/2 stall) and Moderate (MPM@8 → OFDM QPSK R1/2, 0 ACKs, no delivery), while ratiometric picks the robust mode and delivers CRC-clean; the over-read is a channel-independent LEVEL deficit → generalizes to AWGN/Poor. Sim (level AT reference): ~−0.45 dB offset can nudge a boundary pick but is more-conservative and never regresses delivery (gui_qso good@20 QPSK R2/3 PASS with ratiometric default). Opt OUT via `=0`. | **ON** (opt-out `=0`; enables the #71 ≤9 MC-DPSK/DQPSK path on a real radio) | `streaming_sync_acquisition.cpp` `populateDecodeMetrics` (non-OFDM branch) + `connectRatiometricSnrEnabled()` | 🟢 ACTIVE |
| `ULTRA_CONNECT_DATA_AIDED_SNR` | #58 DATA-AIDED FADE-AVERAGED CONNECT SNR (BUG-CONNECT-SNR-VARIANCE increment 2, 2026-07-02). The ratiometric training SNR above is a ~170 ms SNAPSHOT = ONE fade state (Tc ≈ 4.2 s at Good) → rig connect picks spread ~10 dB pick-to-pick (MPG@20: 8.4–18.2). The MC-DPSK demodulator now also computes a DATA-AIDED whole-frame estimate (`updateDataAidedSNREstimate`, `multi_carrier_dpsk.hpp`, sibling of `updateTrainingSNREstimate`): unit-phasor differential chord error vs the config-driven DBPSK/DQPSK constellation, averaged linearly over ~0.2 s blocks across the WHOLE frame (CONNECT = 4 CWs ≈ seconds ≈ multiple Tc) → fade-averaged by construction, zero added handshake latency. Calibration derived, not tuned: magnitude normalization cancels the differential +3.01 dB; geometry-computed non-orthogonal-carrier ICI (−29 dB/carrier @8×1024) subtracted; (k−2)/k inverse-chi-square block correction; +0.5 dB measured residual (table in `test_mcdpsk_snr_calibration.cpp`: AWGN within 1 dB, 0–25 dB, GATED). When ON, `populateDecodeMetrics` routes it as `MCDPSK_IN_BAND` (same basis, lower variance) INSTEAD of the training snapshot — but only when THIS frame's LDPC decode SUCCEEDED (decode-then-measure); else/OFF → training value (byte-identical to increment 1). Logs `MC-DPSK SNR: training=X data_aided=Y (routed=…)` per frame for rig spread analysis. Still subordinate to `ULTRA_CONNECT_RATIOMETRIC_SNR` (whether MCDPSK_IN_BAND routes at connect at all); `connectSelectionSnrDb` +2 untouched (shrink is the planned follow-up after the rig re-measure). | **ON** (opt-out `=0`) | `streaming_sync_acquisition.cpp` `populateDecodeMetrics` (non-OFDM branch) + `connectDataAidedSnrEnabled()`; estimator `src/psk/multi_carrier_dpsk.hpp`; surfaced `mc_dpsk_waveform.{hpp,cpp}` `hasDataAidedSNR()/getDataAidedSNRdB()`, `DecodeResult.mcdpsk_{training,data_aided}_snr_db` | 🟢 ACTIVE |
| `ULTRA_CONNECT_SNR_FADE_BASIS` | #58 CONNECT-TIME SNR BASIS CORRECTION (2026-07-01). The OFDM entry floors/ladder anchors are DIAL-calibrated (forced-rung sim sweeps at `--snr-db`=dial), but the connect-time reading compared against them (#74 ratiometric, above) is FADE-EFFECTIVE-INSTANT: ~2 dB Jensen penalty + fade-phase swing over a ~170 ms ≪ Tc training window. Comparing effective-instant readings to dial thresholds double-penalizes — one fade dip at CONNECT flipped a dial-20 rig channel (sync SNR 21.8, snapshot **12.4** + classifier "Moderate" → floor 14) to MC-DPSK DBPSK ~94 bps = **0 bytes in 9 min**, where a run 10 min later (18.2, "Good") did 1.53 kbps: a zero-delivery coin-flip, stock. `connectSelectionSnrDb()` adds +2 dB to the SELECTION comparison only, fading channels only (`fading_index ≥ kFadingAwgnMax`), at handleConnect/negotiateMode/acceptCall; wire byte + raw logs stay honest (corrected value logs as `SNR_sel=`). MC-DPSK internal floors untouched-by-construction (they were calibrated against the same ratiometric reading, #71 — the +2 there = DQPSK floor −2, which the #71 rig data supports). Cost asymmetry backs the sign (wrong-OFDM at effective ~10 still ~450 bps vs wrong-MC-DPSK 15-40×). RIG-PROVEN 2026-07-01: 3/3 stock MPG@20 picks OFDM QPSK R2/3 CRC-clean (snapshots 17.0/14.6/14.5 → sel 19.0/16.6/16.5), 1.5-1.72 kbps. `=0` disables; (0,6] overrides the 2.0. | **ON (2.0 dB)** | `connection_policy.hpp` `connectSnrFadeBasisDb`/`connectSelectionSnrDb` (above `selectLadderRung`); consumed `connection_handlers.cpp` (handleConnect, negotiateMode), `connection.cpp` (acceptCall) | 🟢 ACTIVE |
| `ULTRA_CONNECT_AFFINE_BASIS` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, sim/rig A/B **PENDING**) — **calibrated AFFINE entry-SNR basis**: replaces the flat `+5` `connectSnrFadeBasisDb` inside `connectSelectionSnrDb`'s fading branch (and both GUI dial-equivalent displays via the new one-source-of-truth `dialEquivalentSnrDb(reading, fading, knob)`) with `correction = clamp(19.55 − reading, +2, +11) dB`. CALIBRATION: 48 rig entries at a KNOWN dial (MPG@20 Watterson Good, docs/CONNECT_ENTRY_CALIBRATION_2026_07_03.md §7) — LS fit of dial on reading is EXACT/DEGENERATE (a=0, b=20, residual 0: within the population the reading is fade-phase noise, σ 3.14, carrying NO dial information); deployed intercept 19.55 = 20 − σ/√48 (one-SE shrink, keeps mid readings off the zero-margin Good QPSK R3/4 anchor 20.0); the clamp is the extrapolation guard outside readings [6.2, 19.4]. SCOPE: **data-aided readings only** (the calibration population) — training-snapshot readings keep the flat basis (fade-crest over-read, MPM@8 safety); the Moderate saturation bound is unchanged and stays keyed to the **raw** reading (≥6.5 zone). EFFECT ON (Good class): reading 9.5 → sel 19.55 → QPSK **R2/3** entry (flat +5 gave 14.5 → R1/2 — the dial-20 opening-minutes bug); readings 4–18 → R2/3, ≥18 → ladder R3/4 (R2/3-capped by `ULTRA_R23_BASIS`); `ULTRA_ENTRY_CAP_R34` R3/4 entry moves to reading ≥21.15. RISK gating default-ON: extrapolation below the calibrated range (a genuine low-dial channel reading 2–6 gets +11 → aggressive OFDM entry) — rig A/B must include a low-dial leg. Unset/`=0` → byte-identical flat path. Read ONCE (static). | **OFF** | `connection_policy.hpp` `connectAffineBasisEnabled()`/`connectAffineCorrectionDb`/`dialEquivalentSnrDb`/`kConnectAffineDialEquivDb` + 4-arg `connectSelectionSnrDb`; displays `app.cpp` status-bar + sidebar "dB eff" sites; tests `test_connection_policy.cpp` `test_connect_affine_basis` | FEAT (A/B) |
| `ULTRA_CONNECT_SNR_POOL` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, #58 increment 3, BUG-CONNECT-SNR-VARIANCE). Connect-SNR POOL: a Connection-hosted ring (cap 8) of qualifying readings (`ConnectSnrPool`, pure-header, population contract enforced in `addReading`: data-aided `MCDPSK_IN_BAND` + `OFDM_BROADBAND` only — the training snapshot NEVER enters) replaces the last-write-wins scalar `measured_snr_db_` as the ENTRY-PICK input. Statistic = decorrelation-clustered **dB-mean** (readings < Tc apart merge to one fade sample; Tc from `retxTroughDopplerHz`→`coherenceTimeMsForDoppler`, same chain as trough pacing — no tuned ms constants; AWGN class ⇒ Tc=∞ ⇒ one stationary cluster). dB-mean (not linear-mean = Jensen double-count, not max = order-statistics bias) is the SAME population the +5 `connectSnrFadeBasisDb` was calibrated on → the basis composes ONCE, downstream, unchanged; the Moderate saturation bound sees a tighter-variance same-centered input. When ON: `rateSelectionSnrDb()` feeds handleConnect/acceptCall/negotiateMode selection + CW pick + the CONNECT_ACK wire byte (the aggregate, never the sentinel) + the `isNearAwgnOFDM` window-16 gate + `shouldUseSingleOFDMFileBlock`. **Increment 4 (2026-07-03, BUG-CONNECT-FADING-VARIANCE) — the knob's scope now ALSO covers the entry FADING:** each pooled reading carries the fading observed with it (`ConnectSnrReading.fading_index`; fed from `setChannelQuality`, NAN for `setMeasuredSNR`-only feeds — never gates admission), aggregated over the SAME Tc clusters by `clusteredFadingIndex` (per-cluster mean, then mean of cluster means — bounded [0,2] statistic ⇒ mean not median); `rateSelectionFadingIndex()` (scalar `fading_index_` knob-off or no fading-carrying reading) feeds the entry pick at the same three sites: `connectSelectionSnrDb`'s fading arg, `coherenceAdjustedFadingIndex` → `selectLadderRung`/`recommendDataMode`/`recommendWaveformAndRate`/`capInitialOFDMRate`, `recommendCWCountForChannel`, `shouldDeferConnectPick`, `selectNegotiatedMode`, and the CONNECT_ACK fading wire byte + entry notify (same treatment as the SNR byte). NON-entry uses of `fading_index_` (ARQ window sizing, file-block sizing, `wireFadingIndex` freshness, Tc derivation) intentionally keep the live scalar. Fed inside `setMeasuredSNR`/`setChannelQuality` exactly where the scalar is assigned (trust/addressing surface identical to main); aged from `Connection::tick` (modem-time, never wall-clock); cleared in `reset`/`enterDisconnected`. Pool still accumulates silently when OFF — output-identical. Rig baseline it targets: 12 connects at dial MPG@20 read 3.9-17.9 (σ 3.15); fading: 48 dial-MPG@20 entries read 0.24-0.74 (σ 0.129), single-frame false-Moderate 18.8% → projected 7.8%/4.1% at N_eff=2/3 (docs/CONNECT_ENTRY_CALIBRATION_2026_07_03.md). UNVALIDATED (edits-only session): needs ctest + knob-off byte-identical gui_qso + the rig ≥10-connect entry-distribution bench (pass: 0 sub-OFDM picks, spread ≤6 dB, per-connect σ ≤2.2; fading: 0 false-Moderate entries in ≥10 Good connects). | **OFF** | `connection_policy.hpp` `ConnectSnrPool`/`connectSnrPoolEnabled()` (above `connectSnrFadeBasisDb`); `connection.hpp` `connect_snr_pool_` + `rateSelectionSnrDb()`/`rateSelectionFadingIndex()`/`rateSelectionSnrDataAided()`; consumed `connection_handlers.cpp` (handleConnect, negotiateMode), `connection.cpp` (acceptCall, `configureArqForCurrentDataMode`, `sendFile`); tests `test_connection_policy.cpp` `test_connect_snr_pool_*` + `test_connect_fading_pool_aggregate` | FEAT (A/B) |
| `ULTRA_CONNECT_PICK_DEFER` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, #58 increment 3; requires `ULTRA_CONNECT_SNR_POOL`). N_eff==1 PICK DEFER, auto-accept only: when the entry pick rests on ONE effective (decorrelated) fade sample on a fading channel AND would land sub-OFDM (MC-DPSK — the 15-40× cost-asymmetry zone; W3's lone 3.9 dB trough reading bought a ~90 bps DBPSK session on a dial-20 channel, P≈8% observed at MPG@20), withhold CONNECT_ACK ONCE (`shouldDeferConnectPick`, pure unit-tested predicate) and let the initiator's EXISTING CONNECT retransmit (10-attempt budget, spacing ≥ ~2 Tc on Good) re-enter handleConnect with a decorrelated second reading (N_eff=2; both samples in deep troughs P≈0.6-0.8% — ~13× fewer catastrophic entries; genuinely-weak channels confirm low twice and keep the MC-DPSK fallback). Fully wire-compatible: a deferred CONNECT is indistinguishable from a decode failure to the initiator; worst case (no retry arrives) = today's lost-CONNECT; cost = one CONNECT cycle (~10-15 s) vs a session-long 20-40× mis-pick. One-shot per handshake (`connect_pick_deferred_once_`, cleared in reset/enterConnected/enterDisconnected); manual `acceptCall` never defers (operator override). Grep: `CONNECT pick DEFERRED once`. UNVALIDATED (edits-only). | **OFF** | `connection_handlers.cpp` handleConnect (between `ladder_selected` and the apply); `connection_policy.hpp` `shouldDeferConnectPick`/`connectPickDeferEnabled()` | FEAT (A/B) |
| `ULTRA_WIRE_SNR_FRESH` | 🟡 EXPERIMENTAL (A/B knob, 2026-07-03, #58 increment 3, MODE_CHANGE wire-SNR staleness fix). The six mid-stream `requestModeChange` embeds pass `wireSnrDb()` = pool dB-mean over readings younger than **3·Tc** (channel-adaptive: Good ~12.7 s, Moderate ~2.5 s) instead of the frozen scalar — mid-transfer the SENDER decodes almost nothing (tone-burst ACKs feed no SNR), so `measured_snr_db_` freezes at sparse control-frame snapshots (rig W2: a 3.2 dB handshake-era trough reading shipped ~31 s stale at t=61.5; 16.5/22.0 re-sent verbatim across 40-300 s; peer_fading frozen 0.73 for ~300 s). Nothing fresh → the explicit stale sentinel **−10 dB** = wire byte 0 (`encodeSNR`, `kConnectSnrStaleSentinelDb`), which the receiver ALREADY renders as "peer SNR n/a" (`app.cpp` ≥0 validity gate) — ZERO receiver change; sentinel physically collision-free (no control frame decodes at a true −10 dB). Scope-justified as display/diagnostics-honesty: the wire value feeds NO receiver decision (verified: handleModeChange → logging + notify only), but a healthy link displaying 3.2 dB reads as link-death at 2 AM, and the field is an established cross-end diagnostic (run4b's level asymmetry was root-caused FROM it). CONNECT_ACK embed NOT gated (fresh by construction). OFF → wire bytes byte-identical. Companion (knob-free, same change): the GUI `MODE_CHANGE:` line now labels the SNR source `wire_peer` vs `local_measured` via the new `snr_is_wire` flag through `DataModeChangedCallback` — the responder connect-time line was a mislabeled LOCAL reading (tell: peer_fading==local_fading). UNVALIDATED (edits-only). | **OFF** | `connection.hpp` `wireSnrDb()` + `kConnectSnrStaleSentinelDb`/`kConnectWireSnrFreshTcMultiple` (`connection_policy.hpp`); `connection.cpp` 6 `requestModeChange` call sites (2 escape-drops, QAM16 demote/step-down/R3-4 walk, EMA change); label fix `app.cpp` DataModeChanged lambda + `tools/ultra_tnc.cpp` signature | FEAT (A/B) |
| `ULTRA_ACK_FADE_EDGE` (opt-out) | §15.5 ACK-staircase FADE-AWARE FAST EDGE + the FEED fix (BUG-ACK-STAIRCASE-FADE-BIN, 2026-07-01). TWO layers were dead: (a) FEED — burst-as-unit delivery bypasses `setRawDataCallback`, so the GUI's lock-free ACK-duration cache froze at the handshake reading (`mcdpsk_in_band` = not a trusted staircase source) for whole transfers → 0% fast-ACK occupancy on ALL channels incl. AWGN@20; fixed by feeding the frontend hook in the burst-group binding BEFORE `onBurstGroupReceived` (which emits the group's ACK) from the decoder's lock-free `last_ofdm_broadband_snr_db_` atomics (new `ModemEngine::getLastOFDMBroadbandSNR()`; engine `LoopbackStats` snr/source are stats-queue-drained and stay stale on this path). (b) EDGE — the 18 dB fast edge was calibrated against the pre-06-16 absolute-referenced meter; the fade-effective basis reads ~16-17 at Good@20 → `symbolMsForSNR(snr, fading_present)` fast edge = **16 dB on fading / 18 dB AWGN** (`kFastAckEdgeFadingDb`), fed by the new `cached_fading_index_` atomic; ONLY the top edge moves (lower rungs are detection-safety-side). The 12 ms rung's HW proof (06-15 MPG@20, 0 retx/15 bursts) is the same physical point that now reads 16-17. Every decision logs `ToneBurstAck staircase: symbol_ms= snr= src= fading=`. MEASURED: occupancy 0%→~90% sim (16QAM 27/2, stock 26/1, AWGN 23/0; edge halves residual slow ACKs vs edge-off 6→2); **rig MPG@20 27/28, 30/33, 28/28 at 12 ms** (was 30/30 at 675 ms). ~4-5%/cycle. `=0` disables the fading edge only (the feed fix is unconditional). | **ON** (feed unconditional; edge opt-out `=0`) | `tone_burst_constants.hpp` `symbolMsForSNR`; `modem_protocol_binding.hpp` burst-group hook; `app.cpp` ACK callback + `cached_fading_index_` | 🟢 ACTIVE |
| `ULTRA_HARQ_PROVISIONAL` (opt-in) | HARQ PROVISIONAL COMBINE KEYS (2026-07-01; narrowed and repaired 2026-08-03). Soft-combine keys normally require CW0 to decode, so a CW0-dead frame cannot accumulate energy across retries. When ON, a failed CW0 peek may key by the receiver's ARQ-mirror prediction for that burst position. Current scope is deliberately narrow: burst transport, exact decoded descriptor, BI off, valid session callback, no invalidated prediction, and either QPSK R3/4 or the exact measured 8PSK R2/3 cw4/Z81 tuple. Unknown/non-forward cadence and a real greater-than-15-second inter-descriptor gap both block; a sender-announced FULL selective-repair anchor is not an RTO. Context observation includes a tail header, but key use excludes the tail and requires at least one current-group real sender+seq match first. The finalize guard compares sender, seq, CW count, and physical-tail identity; a combined decode that reveals a mismatch also invalidates later positions. Provisional entries are evict-first/TTL-bounded, and `decodeFixedFrame` retries a failed combined CW fresh-only and retains fresh rather than a suspect sum after a provisional double-fail. Historical sim: mismatch 0/212 and combines 28→289/run, goodput +6% inside noise; historical rig poison-loop caused the default-OFF flip before fresh-only rescue existed. Telemetry: `[HARQKEY]`, `HARQ provisional key`, and per-group `[HARQ] keys ...`. | **OFF** (opt-in `=1`; exact 8PSK cw4/Z81 IONOS A/B pending) | `streaming_decode_policy.hpp` scope/cadence gates; `streaming_ofdm_decode.cpp` key fallback; `selective_repeat_arq.cpp` prediction; `soft_combine.{hpp,cpp}` identity/tagging/eviction; `frame_v2.cpp` finalize + fresh-only rescue | 🟡 EXPERIMENTAL |
| `ULTRA_FORCE_MCDPSK_RUNG` | DIAGNOSTIC force (`LOW`/`MID`/`ROBUST`/`STANDARD`): pin the MC-DPSK ladder rung, bypassing `selectLadderRung`'s SNR thresholds, to floor-measure each rung (the DQPSK rungs ROBUST/STANDARD are otherwise UNREACHABLE — `robust_floor` > `ofdm_floor`; STANDARD never returned). #72 (2026-06-29) standardized ALL MC-DPSK rungs on **sps=1024** (was ROBUST_LOW 2048 / STANDARD 512) so control==data baud → the handshake is mutually decodable on every rung; gears now vary only constellation (DBPSK/DQPSK)+rate. NOTE (updated #71 2026-07-01): forcing a rung now delivers files end-to-end — rig-proven forced DQPSK & DBPSK both CRC-clean @ MPG@9 — after the #72 handshake + #73 completion fixes; the earlier "forcing breaks the transfer" gotcha is obsolete. | unset | `connection_policy.hpp` `selectLadderRung` (top); `ladderRungForId` | FORCE/DIAG |
| `ULTRA_MCDPSK_WINDOW_CAP` | DIAGNOSTIC override (=N): further cap the MC-DPSK selective-repeat window for A/B round-trip measurement per rung. #71 (2026-07-01): the PRODUCTION default cap was lowered **5→3** (`kMaxRoundTripSafeMCDPSKWindow`) because the airtime-only sizing (`mcDpskWindowSizeForTiming`, 19 s TX-burst budget) was blind to the receiver decode + ACK round-trip — DQPSK's short frames (data_ms 3691) let the window grow to 5, whose ~18.9 s burst's ACK round-trip intermittently exceeded the ~45.7 s RTO → the sender blind-resends the whole window (0 cw_fail, ACKs bitmap=0, all cause=timeout) → spiral. Rig-measured @ MPG@9: DQPSK w5 delivered **1/3**, w3 delivered **3/3** CRC-clean at ~2x DBPSK. DBPSK unaffected (already caps at 3). Knob caps below the computed value; unset = no-op. Companion selector change: Good/AWGN DQPSK **selection floor** lowered `robust_mid_floor+2.5→+1.0` (Good 8.5→7.0, AWGN 7.5→6.0). | unset (default cap = 3) | `connection_policy.hpp` `mcDpskWindowSizeForTiming` + `selectLadderRung` | FORCE/DIAG |
| `ULTRA_TX_LEADIN_MS` / `ULTRA_TX_TAIL_MS` / `ULTRA_TX_ACK_LEADIN_MS` | configurable TX guard timing (#68). `postProcessTx` prepends a lead-in + tail of silence to every TX (PTT/PA-ramp/ALC settling so the chirp isn't clipped at key-up — RX does not need it). The legacy FIXED 150ms lead-in + 50ms tail (= 200ms/TX, on data + ACK + ping; the ACK's copy is in the half-duplex turnaround) is now tunable: `ULTRA_TX_LEADIN_MS`/`ULTRA_TX_TAIL_MS` global defaults, `ULTRA_TX_ACK_LEADIN_MS` overrides JUST the ACK (the turnaround-relevant, lowest-PA-thermal piece). Default-UNCHANGED (unset → 150/50, byte-identical). The configurability is the radio-agnostic fix (150ms is a one-size early-project guess, 2-5× over real radios IC-7300/FT-891 ~15-20ms T/R). FIDELITY CAVEAT: the cheap-card rig has no real 100W PA → a reduction can't be rig-validated (real PA ramp could clip first symbols); also the ACK lead-in gives the data-sender T/R-to-RX time (too short → missed ACK). Conservative-by-default until real-radio-proven. | **150/50/−1** (default-unchanged) | `modem_engine.cpp` `postProcessTx` (lead-in/tail); `transmitToneBurstAck` (ACK lead-in) | FEAT (config) |
| `ULTRA_WARM_TURNAROUND_OFF` (opt-out) | make the connected-OFDM pre-TX echo-clear (`clearRxBuffer(for_tx_echo=true)`, fired before every ACK turnaround) a decoder NO-OP, preserving warm-sync state + ring timeline across the half-duplex turnaround instead of `reset()`-wiping them (which forced a COLD 2.5s re-acquire of every next burst — the ~27% Good/Moderate turnaround tax). The audio side (`setRxMuted`+`stopCapture`+`AudioEngine::clearRxBuffer`) already prevents echo, so the decoder full-reset was harmful overkill; the no-op makes the rig behave like OTASim (which skips this path entirely, app.cpp:3040). Correct by construction — worst case = stale prediction → cold fallback (== old behavior), never a stranded frame. Rig-proven CRC-clean on BOTH Good (MPG@20: turnaround 2.71→1.54s median, −43%) AND Moderate (MPM@20: 1.59s, 0 stalls). The faithful gate CANNOT exercise this path (sim TX skips it) so it's a no-op on OTASim/ctest either way. The OTASim ~0.6s (warm predict-and-wait) is partly a SIM ARTIFACT (±20ms window vs ±260ms real inter-group jitter) — NOT a target. | **DEFAULT-ON** (2026-06-20; `ULTRA_WARM_TURNAROUND_OFF=1` to disable) | `modem_engine.cpp` `clearRxBuffer`; call site `app.cpp:3051` | 🟢 ACTIVE |
| `ULTRA_TNC_BULK_ACCUM` | TNC: hoard a flow-controlled PAT/B2F body (under-report BUFFER to cap 50 while absorbing so PAT keeps feeding past its 7×blocksize throttle, + 20 s BUFFER keepalive vs PAT's 60 s Flush timeout) → flush the whole body as ONE z=81 burst-file instead of <4 KB short-LDPC chunks. Body bursts+decodes CRC-clean; trailing FF blocked on BUG-TNC-B2F-002 | **OFF** (default; experiment) | `tnc_session.cpp` ctor + `onModemBufferLevel`/`tick`/`handleDataBytes` | FEAT (blocked on BUG-TNC-B2F-002) |
| ~~`ULTRA_S16_WARM_HANDOFF`~~ | warm light-LTS group-start | **REMOVED 2026-05-31** — promoted to production default (always on); all 7 gate sites unconditional | — | ✅ codified |
| `ULTRA_S16_TRACE_WARM_WINDOW` | trace warm-sync window | off | `streaming_sync_acquisition.cpp:356` | DIAG |
| `ULTRA_LOCK_RATE` | hold data rate fixed for transfer (`=1` pins; overrides the 2026-07-02 default-ON fade-riding ladder — the fixed-rung baseline/probe switch, also un-defaulted in `gui_qso_scenario.sh` same date) | OFF | `connection.cpp` `rateAdaptationActive` | FEAT |
| `ULTRA_AUDIO_BUFFER` | GUI/TNC SDL audio device buffer (samples/period; device double-buffers → total latency ≈ 2×). Smaller = lower half-duplex T/R turnaround latency (faster ACK round-trip → higher file goodput) but more underrun risk on slow hosts. Live pi5tnc QSO vs sim: identical 10 bursts/74.7 s airtime, but turnaround 3.6 s vs 1.1 s ⇒ ~1.3 s is buffer latency at 8192. Clamp [64,16384]. | **8192** (~170 ms @ 48 kHz) | `audio_engine.cpp` ctor → `setBufferSize` | FEAT |
| `ULTRA_MAX_OFDM_RATE` | cap initial+adaptive rate | unset | `connection.cpp:673` | FEAT |
| `ULTRA_ENABLE_QAM16_LADDER` | let the AUTO ladder SELECT 16QAM (Phase 1, 2026-06-12). ON → `selectCoherentOFDM` walks `kCoherentLadderQAM16Exp` with the {QAM16,R1/2} GOOD rung enabled at the Phase-0a measured floor (18 dB, EXPERIMENTAL zero-margin). Default ladder byte-identical when unset. Paired with `maxValidatedCoherentRate(mod)` per-mod rate cap. **2026-06-14: cap lifted QAM16→R2/3 + the 16QAM R2/3 Good@20 rung ENABLED** — cross-frame TIME interleave (auto-on for 16QAM) makes 16QAM R2/3 Good@20 deliver ~2033-2240 (ABOVE QPSK R3/4 ~1860); adaptive ladder GUI-verified to auto-pick 16QAM R2/3. No longer "lateral". | **OFF** | `waveform_selection.hpp` `qam16LadderEnabled()` / `kCoherentLadderQAM16Exp` (R2/3 Good@20) / `maxValidatedCoherentRate`(QAM16→R2/3) | FEAT (Phase 1+2b; 16QAM R2/3 now beats QPSK R3/4 on Good) |
| `ULTRA_FRAME_CW` | override CW/frame | unset | `connection.cpp:727` | FEAT |
| `ULTRA_LDPC_Z` | Z=81→n=1944 vs 27→n=648. **16→1 sites (2026-05-30, DONE)**: RX via descriptor (`activeBurstLiftingZ()`), TX consumers via `ldpc_lifting_z_`, chunker + `makeFixedDataFrame` + `modem_engine` via `Connection::selectBurstLiftingZ()` (app/cli push it to the encoder). Z is code-derived, written to the descriptor, read back. **Transport Merge 2026-06-06: the unified file path uses z=27** — `selectBurstLiftingZ` gates z=81 on `!kUnifiedSeqEnabled()` (always false) so z=81 long-LDPC is now dead (was the legacy `burst_transport_` file behavior); z=27 doubles as the descriptor's "regular frames" signal. The 1 remaining read is the discovery override INSIDE the policy. | 27 (always) | `connection.cpp` (override only) | FEAT (derived) |
| `ULTRA_LEGACY_OFDM_GROUP_ACK` | old OFDM GROUP_ACK vs tone-burst | OFF | `connection.cpp:445` | FEAT (A/B) |
| `ULTRA_SOFTWARE_ALC` (opt-out) | closed-loop TX-drive (software-ALC, BUG-QAM16-RIG-LEVEL-BUDGET): receiver's per-burst level verdict (LOW = data < `ULTRA_ALC_LOW_DB` over chain noise for 2 consecutive bursts → advise up; CLIPPED = crest factor < `ULTRA_ALC_CLIP_CF_DB` → advise down NOW) rides the tone-burst ACK bits [30..31] (**wire-breaking 2026-07-02, CRC coverage 18→20 bits — lockstep builds**); sender walks per-connection tx_drive +0.5 dB/−2 dB clamped [settings baseline, 0.85 `kSoftwareAlcMaxPeakTarget`], connected OFDM data bursts ONLY (handshake/control/MC-DPSK keep baseline), one step per ACKed group, reset at CONNECT/DISCONNECT. `=0` disables the LOOP both ends; the receiver `LEVEL ADVISORY:`/`[ALC-RX]` log stays on. Sim: default TX path RMS-normalizes → loop is a no-op (gate-proven zero moves at good@20); `ULTRA_SIM_PAPR_PENALTY=1` path uses the walked drive (rig-free A/B). | **ON** | `connection_policy.hpp` `softwareAlcEnabled` + thresholds; `streaming_burst_interleave.cpp` `computeBurstLevelVerdict`; `connection.cpp` SACK-emit stamp + `onToneBurstAck` hook; `app.cpp` `handleDriveAdvisory`/`effectiveTxDriveForContext` | FEAT |
| `ULTRA_ALC_LOW_DB` | software-ALC LOW verdict threshold: burst data RMS over idle chain-noise floor (dB). Default derived: rig level-limited state measures 6-7 dB vs ~20 dB healthy sim reference; ≥12 dB ⇒ chain noise costs ≤0.27 dB (no longer the bottleneck); 0.5→0.85 walk (+4.6 dB) converges from 6-7 to ~11-12 = the threshold. Accepts (0,40). | **12.0** | `connection_policy.hpp` `alcLowHeadroomDb` | ADAPT |
| `ULTRA_ALC_CLIP_CF_DB` | software-ALC CLIPPED verdict threshold: burst crest factor (dB) below which upstream clipping is assumed. Default derived: healthy OFDM arrives CF 9-14 dB (Gaussian max-stat ~12-13 dB at burst length; wire-measured 9-14), noise-dominated segments ~10-12 (no false CLIPPED when buried), hard clipping collapses to 0-6 (IONOS 2026-06-15 CF=1.01). 6.5 = ≥2.5 dB under the healthy floor. Accepts (0,12). | **6.5** | `connection_policy.hpp` `alcClipCrestFactorDb` | ADAPT |
| `ULTRA_SHORT_REANCHOR_CHIRP_MS` | short re-anchor chirp ms [100,300] | 100 | `connection_policy.hpp:380` | FEAT |
| `ULTRA_SHORT_ANCHOR_DESCRIPTOR_MS` | Phase 2a (2026-06-14): warm BURST_HEADER descriptor uses a SHORT DUAL chirp (per-chirp ms, gap 50) instead of the full 500 ms dual — reclaims ~600 ms/burst chirp airtime. Per-chirp ms, clamp [50,600], 0=off. **Channel-gated to R3/4** via `connection_policy::shouldUseWarmShortAnchorDescriptor` (coherent OFDM_CHIRP top rung only — single-chirp/aggressive-shortening crater on fading; the bad seed relocates with duration). Cold acquire / first-burst / resends keep the full dual (miss self-heals). GUI-verified knob=250: Good@20 R3/4 GATE=ON descr 38880 = 2110 bps (+7%); Mod@14 R1/2 GATE=OFF descr 67680 = 1040 bps (crater suppressed). Default OFF / byte-identical. | **OFF** | `ofdm_chirp_waveform.cpp` `shortAnchorChirpMs()` / `getShortAnchorChirpConfig()`; gate `connection_policy.hpp` `shouldUseWarmShortAnchorDescriptor`; wired `streaming_encoder.cpp` (descriptor) | FEAT (Phase 2a; default-off until full mod×rate×channel matrix) |
| `ULTRA_ANCHOR_SKIP_K` | #69 periodic full-chirp anchor + warm-skip — **SKIP not SHRINK** (`project_chirp_anchor_skip_not_shrink`). The full dual chirp (TB=1200 = fade margin) is near-optimal so shrinking it craters; instead emit it only every K groups (`burst_anchor_ordinal_ % K == 0` — a monotonic per-descriptor counter, NOT the ARQ base seq) and give skipped groups a LIGHT LTS-only (chirp-less) descriptor that rides the warm-predict + light-LTS path. Full chirp every K = robustness backstop + drift reset; session-first + resends keep the full chirp. **Wire flag** `BURST_FLAG_NEXT_LIGHT_ANCHOR` (frame_v2.hpp payload[4] 0x04) announces the next group's anchor so the RX full-/light-searches immediately (no grinding through light rejects — the K=3 crawl); K-gated fast §16.4 escalation (4 rejects vs 12) catches unannounced reactive resends. Rig MPG@20 10-run interleaved: **K=2 +10.5% mean / +12.8% median, 0 stalls**; K=3 banks no gain → **K=2 = sweet spot**. **GATED by the REACTIVE clean-streak (see `ULTRA_ANCHOR_SKIP_CLEAN_STREAK`)** — the rig DISPROVED a predicted coherence-disc gate (a Moderate channel read clean-Good for a whole transfer, #57), so the skip engages on OBSERVED delivery instead. **DEFAULT-ON 2026-06-20 (default 2, opt-out `ULTRA_ANCHOR_SKIP_K=1`):** rig-proven safe (MPG@20 + MPM@20, STALL=0, reactive revert holds under load) + safe-by-construction (resend→full chirp→can't be worse than K=1); throughput net tie-to-positive (Good A/B +1.9% mean, +8% clean pair, −3% fady pair). NOT a proven throughput win — default-on for safety + free clean-channel upside + duty savings. | **2 (DEFAULT-ON; =1 opts out)** | `streaming_encoder.cpp` `encodeBurstLight` (skip schedule + reactive streak) + `encodeFrame` (light preamble); `streaming_ofdm_decode.cpp` (stash announcement); `sync_controller.cpp` `noteGroupDelivered`/`detectConnectedLightSync` (arm search + K-gated escalation); `frame_v2.hpp` (flag + parse) | FEAT (#69 prototype; reactive-gated) |
| `ULTRA_REACTIVE_SHORT_CHIRP` | #62 reactive SHORT dual chirp. On a chirp-bearing (non-skipped) group, emit a SHORT dual chirp (duration = `ULTRA_SHORT_ANCHOR_DESCRIPTOR_MS`, e.g. 200/250 vs full 500) on a clean reactive streak, revert to FULL on crater. Reuses the short-anchor infra but REACTIVE-gated (reactive_skip_enabled) not R3/4-gated. RX needs NO change (detectSync auto-falls-back to the short detector, ofdm_chirp_waveform.cpp:478). **OVERNIGHT RIG VERDICT 2026-06-21 (~50 runs MPG@20, all CRC-clean STALL=0): SAFE WASH, not a goodput win.** Works on the real cheap-card rig (RX detects ~81-92%); but a detection miss costs a ~group resend (~14× a chirp's 0.65s saving) so it's net-positive only at ~99%+ detect, and the rig caps at ~92% (8% miss irreducible). Cross-phase short-vs-skip ~−1% (wash within ±25% noise); ~3% airtime saved is below the goodput floor. KEEP env-gated default-OFF (safe duty/thermal option). | **OFF** (default-off; safe wash) | `streaming_encoder.cpp` `encodeBurstLight` (reactive short-anchor gate) | FEAT (#62; investigated to ceiling — wash) |
| `ULTRA_SHORT_CHIRP_DETECT_SCALE` | #62 scales DOWN just the short-anchor fallback detector threshold (`detectSync`, ofdm_chirp_waveform.cpp:479) to catch more (lower-TB → lower-peak) short chirps. **FAILED EXPERIMENT (Phase 5 rig): scale 0.6/0.4 → RX detect count EXCEEDS sent count (180%/167% = FALSE POSITIVES) → mis-anchors → rtx 24→55 → goodput −11/−13%.** Detection can't be pushed past ~92% without false alarms. Keep default 1.0. **FOOTGUN — REMOVAL candidate** (only hurts). | **1.0** (default byte-identical; <1.0 backfires) | `ofdm_chirp_waveform.cpp` `detectSync` (short fallback) | FEAT (#62; proven counterproductive — remove) |
| `ULTRA_ANCHOR_SKIP_CLEAN_STREAK` | #57 REACTIVE anchor-skip gate (radio-agnostic, delivery-driven). Replaces the rig-disproven PREDICTED coherence-disc gate: a Moderate IONOS channel read clean-Good (coherence-area +0.20, raw acf lags1-5 +0.2-0.3) for a whole ~60 s transfer → a predicted label cannot safely gate the skip (a 60 s transfer is ~10-20 fade cycles = too few to pin Doppler; non-stationary). Instead, `anchor_skip_clean_streak_` counts consecutive clean (warm-delivered) groups; ANY resend/cold-start/§16.4 escalation (`warm_descriptor==false`, the connection forces a full chirp) RESETS it to 0; the skip engages only once streak ≥ this value, and reverts to full-chirp-every-group the instant a resend fires. Half-duplex ACK turnaround → `warm_descriptor==false` reflects the prior group's delivery (delivery-driven, not send-optimistic). The streak-reset IS the cooldown + auto-covers cold-start. Sim Good K=2: 5-group full-chirp warmup → reactive=ON → CRC-clean 1940 bps. The Stage-A coherence-area metric (`doppler_coherence_estimator.hpp::coherenceArea`) is kept as READ-ONLY telemetry only. | **4** (only active when `ULTRA_ANCHOR_SKIP_K`>1) | `streaming_encoder.cpp` `encodeBurstLight` (streak gate); `streaming_encoder.hpp` (`anchor_skip_clean_streak_`) | FEAT (#57; the gate that makes anchor-skip default-on safe) |
| `ULTRA_WIENER_DELAY_SPREAD_S` | Wiener freq-corr delay spread | **hardcoded 1e-3 (Moderate-HF) — NOT derived; adaptivity gap** | `channel_equalizer_pilot.cpp:28` | ADAPT |
| `ULTRA_WIENER_DOPPLER_HZ` | Wiener time-corr Doppler | **hardcoded 0.5 Hz (Moderate-HF) — NOT derived; adaptivity gap** | `channel_equalizer_pilot.cpp:35` | ADAPT |
| `ULTRA_PILOT_DFT_INTERP` | delay-domain (CIR) pilot reconstruction for the data-carrier channel. Replaces the frequency-smoothness prior with a delay-support prior: positions observations by **physical** carrier index `k` (the default path uses LOGICAL index, which is off by one carrier across the DC gap) and correlates with the **Dirichlet kernel** of an integer-sample delay window — the closed form of "IDFT → truncate CIR to its support → DFT back" for this non-uniform, DC-gapped pilot grid (an FFT is the wrong tool here: `Np*spacing != fft_size` and logical index isn't proportional to frequency). Window `W = min(span·f_s, cyclic_prefix, fft_size/pilot_spacing)`, forced odd; the last cap is the unambiguous delay range (two delays differing by `fft/d` are identical at every pilot). Applies to BOTH reconstruction paths — swaps the Wiener frequency axis when scattered pilots are active, replaces the linear interpolation when they are not. Re-read **per OFDM symbol** (not a latching `static const`) so one binary runs both A/B arms. **MEASURED NEUTRAL 2026-07-28** (`measure_ack_fer data4_full`, 6 seeds × 24, ITU Good): 16QAM R2/3 51.4/30.6/21.5/16.7/15.3% @20/24/28/32/40 → 52.1/30.6/21.5/15.3/15.3 ON; no move on 8PSK/QPSK/AWGN/Moderate or at pilot spacing 10/12 either. **Conclusion: the pilot→data reconstruction filter is NOT the 16QAM Good limiter** — see CHANGELOG 2026-07-28. Kept default-OFF as an instrument. | **OFF** | `src/ofdm/delay_domain_interpolator.hpp`; wired `channel_equalizer_pilot.cpp` `updateChannelEstimate` + `estimateWienerChannel`; members `demodulator_impl.hpp delay_domain_interp_`/`delay_domain_window_` | DIAG/AB (measured neutral; instrument for the pilot-geometry investigation) |
| `ULTRA_PILOT_DFT_SPAN_MS` | delay-window span override for `ULTRA_PILOT_DFT_INTERP` (ms, accepts (0,20)). Default = the SAME channel delay assumption the Wiener prior uses (`wiener_delay_spread_s_override_` from the live channel-class verdict, else `ULTRA_WIENER_DELAY_SPREAD_S` / 1 ms), so the knob swaps the reconstruction FILTER and not the channel MODEL. Sweeping it 0.05→2.0 ms moved 16QAM Good@32 by 0 frames in 72 — part of the evidence that the filter is not the limiter. | derived (1 ms) | `channel_equalizer_pilot.cpp` `updateChannelEstimate` | DIAG/AB |
| `ULTRA_DD_FADING_MAX` | fading-index gate for DD (BUG-8PSK-001) | 0.15 | `channel_equalizer_pilot.cpp:927` | ADAPT |
| `ULTRA_QPSK_DD` | opt-in DD tracking for QPSK | OFF | `channel_equalizer_pilot.cpp:891` | ADAPT |
| `ULTRA_COHERENT_DD_OFF` | disable all coherent DD | DD on | `channel_equalizer_pilot.cpp:901` | DIAG/AB |
| `ULTRA_REL_FADE_ONSET` / `_MAX` | relative-fade LLR inflation (QPSK/QAM8) | code default | `channel_equalizer_equalize.cpp:104/111` | ADAPT |
| `ULTRA_LLR_NOISE_EMP_FLOOR` | empirical post-eq noise-var floor (single-symbol hard-decision; measured NET-NEGATIVE 2026-06-12, superseded by `ULTRA_HERR_LLR_K`) | 0 (off) | `channel_equalizer_equalize.cpp:618` | DIAG |
| `ULTRA_HERR_LLR_K` | per-carrier channel-estimate-error (ε²_H) LLR term: `nv=(σ²+k·err_var·|H|²)/(|H|²+σ²)` using the Wiener interpolator's normalized MMSE residual (the pilot-anchored production form of EMP_FLOOR). Down-weights confident-wrong LLRs on poorly-estimated carriers near freq-selective nulls; inert on flat channels (err_var≈0). **GUI-validated 2026-06-12**: 16QAM R2/3 sp8 Good@20 loss 55→45% / +20% goodput 3/3 seeds; QPSK/AWGN no-regress; 16QAM R1/2 +3-8%. **k=1.0 VALIDATED** (k-tune 0.5/1.0/2.0 Good@20: peaks at 1.0; k=2.0 over-inflates, CW-fails spike; k=0.5 under-weights — the Wiener error_var is a calibrated variance, trust it 1:1). **DEFAULT-ON k=1.0 since 2026-06-17 (a10d4ef), SCOPED to QPSK/QAM8** (`!soft_gray_zone_csi`): cuts Mode-B confident-wrong CW-fails ~86% (controlled OTASim moderate@20). QAM16 EXCLUDED — stacks on `softGrayZoneNoiseInflation` → double-count → regression (QAM16 good@24 2720→2020 bps). `=0` disables. | **1.0 (ON, QPSK/QAM8 only)** | `channel_equalizer_equalize.cpp` `equalize()` (gated `!soft_gray_zone_csi`); populated `channel_equalizer_pilot.cpp` `updateChannelEstimate`; member `demodulator_impl.hpp per_carrier_h_error_var_` | ADAPT (Phase 2b; the unified per-carrier reliability model) |
| `ULTRA_ITERATIVE_CHEST` | 🟡 EXPERIMENTAL (A/B knob, default-OFF; 2026-07-29, CHANGELOG). **POST-FEC data-aided channel estimation.** ⛔ ITS ORIGINAL RATIONALE IS RETRACTED (2026-07-29, CHANGELOG): `ULTRA_GENIE_DATA_AIDED` sets Ĥ=Y/X, and the MMSE equalizer then collapses to X exactly with the noise divided out — a NOISELESS SYMBOL ORACLE, not perfect CSI (proof: it decoded QPSK R3/4 at ~4.7x Shannon capacity at −6 dB). "The wall is ESTIMATION" is NOT established. ⛔ AND NEITHER IS THE CONVERSE (2026-07-29): the counter-evidence `ULTRA_GENIE_LTS_FREEZE` is ALSO an invalid oracle — it is NOT "true full-band frequency CSI" but ONE noisy LTS snapshot held across the frame in place of per-symbol pilot tracking, so its harm is lost noise-averaging, not a verdict on perfect CSI (its own apply-site comment states the condition: "on a NOISELESS frozen channel"). Staleness excluded: J0(2π·0.1·0.35)≈0.988 over a frame at ITU Good. Control: on AWGN@25 (CONSTANT channel, so freezing is exactly right) it is byte-identical to baseline 19/20 — plumbing sound, fading harm = estimation noise. BOTH oracles invalid ⇒ the estimator question is OPEN IN BOTH DIRECTIONS; cite neither. This knob stands on its own mechanism instead — a parity+CRC-verified frame yields an exact, model-free, noise-limited `H_dd=Y/X` — and is a MEASURED NULL kept for its two findings. A frame that passes LDPC parity on **every** codeword AND the frame CRC AND used no perturbation retry tells the receiver exactly what was sent: re-encode via the PRODUCTION `v2::encodeFixedFrame` on `cw_status.reassemble()` (the deterministic PRBS pad regenerates, so it is exact), re-modulate via the PRODUCTION `modulate()` path, and push `H_dd = Y/X` into the EXISTING separable Wiener. **CARRY-FORWARD, not an intra-frame second pass, and that is structural:** `FrameInterleaver` (`frame_interleaver.cpp:46`) spreads any CW consecutive air bits across all CW codewords, so a PARTIALLY decoded 16QAM cw4 frame determines **0.000** of its carriers (QPSK 0.50, 8PSK 0.375) while a fully decoded one needs no help — the value is transferring one frame's exact channel to the NEXT frame of the group. NOT the pre-FEC DD tracker (`dd_qam16_*`, measured FLAT 31/150 and harmful on 8PSK per BUG-8PSK-001): post-FEC X is verified, so `H_dd`'s error is independent of `Ĥ`. **Adaptivity is by construction** — the only modulation-dependent quantity is `noise_norm_dd = wiener_noise_norm/|X[k]|²` (from `H_dd = H + N/X`; all constellations are unit-average-power ⇒ 1× for QPSK/8PSK, +7.0 dB on 16QAM inner points, correct for 32/64/256-QAM unasked). Three prerequisites, all guarded so knob-off is byte-identical: ABSOLUTE Wiener time axis (`wiener_symbol_base_`; the frame-relative index would present a 0.35 s-old sample at Δt=0 at ρ=1.0), FLAT storage domain (per-frame LTS timing ramp), and **COMMON-PHASE RE-REFERENCING** — each frame is acquired independently so `H_k = H_true·e^{jφ_k}` with φ arbitrary; MEASURED at −75°..+23° frame-to-frame, and without the correction the GUI gate read ~865 bps and no delivery vs 1840 baseline. Correction = ML 1-parameter fit `φ = arg(Σ H_new·conj(H_carried))`, constant-free. GATED OFF when `use_burst_interleave_` (a codeword then spans the whole group ⇒ no frame's X is reconstructable) and on any pass without a fresh absolute origin. CPU MEASURED on a **Pi5**: 2.8-5.0 ms/frame = **0.49-1.40% of frame airtime** (Mac 1.0-1.9 ms) — real-time safe. **Before default-on: the GUI-gate multi-seed A/B is INCONCLUSIVE — this working tree is flaky on BOTH arms (BUG-GUI-GATE-EARLY-EXIT-FLAKE; baseline seed 11 died at 21 s with the same signature), so the reliability delta is UNMEASURED. Delivered pairs only: s42 2100 vs 1840 (+14.1%), s23 1540 vs 1630 (-5.5%) => mean +4.9%, INSIDE the gate's +/-25% fade noise. Do NOT quote it as a win; fix the gate first.** | **OFF** | `src/ofdm/iterative_chest.hpp` (knob, non-latching); `ofdm_stream_processor.cpp` (Y retention, `wiener_symbol_base_`, carry gate); `channel_equalizer_pilot.cpp` (`ingestDataAidedGrid`, `rereferenceCarriedHistoryPhase`, flat domain); `ofdm_chirp_waveform.cpp` (`remodulateDataCarrierSymbols`, `ingestDataAidedFrame`); `streaming_ofdm_decode.cpp` (verified-frame re-encode); `streaming_burst_interleave.cpp` (arm/origin/ingest, group-end clear); test `test_iterative_chest_remod.cpp` (`IterativeChestRemod`) | FEAT (A/B) |
| `ULTRA_LTS_DFT_DENOISE` / `_TAPS` | 🟡 EXPERIMENTAL, **default-OFF and now MEASURED A NULL — keep it off**. Gaussian across-carrier smoothing of the LTS H. It genuinely works as an ESTIMATOR: verified 2026-07-29 with the truth-referenced `--chest-nmse` diagnostic on frozen taps, **4.05x** lower channel-estimate NMSE at QPSK R3/4 (0.02675→0.00660) and **3.97x** at 16QAM R2/3 (0.02492→0.00628), corroborating the offline harness's 3-6x claim. **But that 4x better channel mean buys ZERO FER**: ITU Good, 4 seeds x n=60 = 240 frames/cell, deltas of +1/-1/0/+3 at 20 dB (QPSK R3/4, 8PSK R2/3, 16QAM R2/3, 16QAM R3/4) and +0/+0 at 14 dB, +0/+2 at 11 dB (QPSK R3/4, R1/2) — every one inside 1σ≈7.7. **This is the EFFECTIVE_SINR §9 Phase 3 stopping rule firing** ("if the new channel mean does not materially reduce the target FER, stop treating channel-mean estimation as the main speed limiter"), and it fires WITHOUT needing the perfect-CSI oracle. Do NOT build the Phase 2-4 posterior/Kalman estimator on the channel-mean rationale. NOT refuted by this: estimator UNCERTAINTY semantics (§2.4, P_H collapsing toward zero in notches where the estimate is least trustworthy) — that is a reliability-weighting lever, not the mean. | **OFF** | `ofdm_demodulator_setup.cpp:34`; A/B via `measure_ack_fer` | FEAT (MEASURED NULL) |
| `ULTRA_LTS_CFO_AVG` | CFO-clean 2-LTS averaging (lever ①) | OFF | `ofdm_demodulator_setup.cpp:39` | FEAT (task #9, in-flight) |
| `ULTRA_FORCE_DATA_MOD` / `_RATE` | force mod/rate, bypass ladder | unset | `waveform_selection.hpp:512/525` | FORCE |
| `ULTRA_FORCE_WAVEFORM` | pin the NEGOTIATED data waveform (`OFDM_CHIRP`/`OFDM_NARROW`/`MC_DPSK`) below its auto entry-SNR — floor probing; both ends set it | unset | `app.cpp:1303` (GUI forced-waveform apply) | FORCE |
| `ULTRA_CHANNEL_DOPPLER_HZ` | override Watterson Doppler (sim) | code default | `ota_channel_core/channel.cpp:178` | FORCE/DIAG |
| `ULTRA_*GENIE*` (data-aided, timing/CFO, channel, sigma) | genie oracles for QAM16 isolation | OFF | `channel_equalizer_pilot.cpp:62/134`, `streaming_ofdm_decode.cpp:103`, `modulator.cpp:309` | DIAG (oracle) |
| `ULTRA_GENIE_DATA_AIDED` | true per-symbol data-aided channel genie `H=Y/X`; TX capture keyed by (frame anchor, symbol index) — see `genie_tx_capture.hpp`. ALIGNMENT FIXED 2026-07-28 (was a FIFO cursor, silently mispaired) | OFF | `genie_tx_capture.hpp`, `modulator.cpp:309`, `channel_equalizer_equalize.cpp:431`, `streaming_encoder.cpp:400/470/750/878` | DIAG (oracle) |
| `ULTRA_CHEST_NOISE_SCALE` | 🟡 EXPERIMENTAL (default-OFF, non-latching). Corrects a REAL 3.01 dB scale error on the decode path: the LTS noise estimate is `E|h1−h0|²/4 = σ²_bin/2` (the AVERAGED estimate's variance), but its consumers — MMSE denominator (`channel_equalizer_equalize.cpp:612,639`), LLR σ² (`:619,690`), erasure gate (`:541`) — all want the per-symbol `σ²_bin`. The METER path already compensates (`channel_equalizer_lts.cpp:751` `2.0f * lts_noise_var`); the decode path does not, so the two disagree by 2×. **MEASURED A WASH** (Good@20, seeds 7/11/23/42, n=100): QPSK R3/4 93.5→93.5, 8PSK R2/3 82.2→81.0, 16QAM R2/3 51.0→49.8, R3/4 27.8→26.8 — all inside σ≈2 at n=400. Kept default-OFF to DOCUMENT the inconsistency so it is not rediscovered; the MMSE regulariser is not the binding constraint. | **OFF** | `src/ofdm/channel_equalizer_lts.cpp` (knob + assignment site) | DIAG/FEAT (A/B) |
| *(no env knob)* `genie_true_h` | 🟡 EXPERIMENTAL DIAG, inert by default (`mode()` defaults to `Off`; a process-global rather than getenv because both passes run in ONE process and the mode must flip between them). Intended VALID perfect-CSI bound: run each frame twice — once through a shadow `SimulatedChannel` with the SAME seed at high SNR (its LTS estimate IS the true H, already in the equalizer's own domain/scale), then inject into the noisy pass. ⛔ **DOES NOT WORK AS BUILT, and the reason is general:** H is only valid inside the FFT WINDOW it was measured in, so any cross-frame/cross-pass injection carries a `exp(−j·2πk·δ/N)` ramp from the differing sync offset. Evidence: engaged correctly (captures=19, injections=1083, misses=0) yet scored **0/20 vs a 19/20 baseline on AWGN@25** — a channel with NO fading; kills DQPSK/D8PSK/16QAM identically (⇒ phase/timing, not amplitude); and the oracle's own null control (shadow SNR == real SNR ⇒ bit-identical signal ⇒ injection must be a no-op) still scored 0/20 with `mean_nmse_vs_replaced=1.67`, the value for two UNCORRELATED vectors. This is exactly why `ULTRA_GENIE_LTS_FREEZE` is sound — it never leaves its own frame. **To finish: suppress channel noise over the preamble+LTS span so the LTS is noiseless WITHIN the single real pass** (reuses the proven in-frame freeze plumbing, no cross-pass domain problem). Harness: `measure_ack_fer --genie-true-h 1 [--genie-clean-snr <db>]`; counters print to stderr — an oracle that silently fails to engage reports "no effect", indistinguishable from "perfect CSI does not help". | **OFF** | `src/ofdm/genie_true_h.hpp`; capture `channel_equalizer_lts.cpp`; inject `channel_equalizer_pilot.cpp`; `tools/measure_ack_fer.cpp` | DIAG (oracle) |
| `ULTRA_GENIE_VERIFY` | independent CONTENT validator for the genie key: quadrant agreement between the production-equalized symbol and the looked-up X (1.000 = aligned, ~0.25 = mispaired). Falsifies the key; never selects an entry | OFF | `channel_equalizer_equalize.cpp:449` | DIAG |
| `ULTRA_GENIE_DEBUG` | per-chunk genie accounting: frames/pushed/lookups/hits/misses/geom_rejects/origin/worst_residual | OFF | `genie_tx_capture.hpp` (`reportIfDebug`) | DIAG |
| `ULTRA_EQ_TRACE` | decoder `equalize()` call-tree tracer (pass ordinal, call site, symbol, absolute position) — how the 2-6 re-decodes per frame were measured | OFF | `genie_tx_capture.hpp` (`EqTrace`), `ofdm_stream_processor.cpp`, `streaming_ofdm_decode.cpp` | DIAG |
| `ULTRA_FAILURE_ATTRIBUTION`, `ULTRA_CONSTELLATION_DIAG`, `ULTRA_CFO_DEBUG_LOG`, `ULTRA_HARQ_DEBUG_LOG*`, `ULTRA_DUMP_CFO_*`, `ULTRA_CARRIER_SENSE_DEBUG`, `ULTRA_STARTUP_LOG`, `ULTRA_E2E_DEBUG_LOG`, `ULTRA_DIAGNOSTICS_DIR` | logging / dumps | off/unset | various | DIAG |

---

## 7. Cleanup register (actionable — current valid infra vs removable)

> Deletions are tracked as a focused action list in **`docs/REMOVAL_BACKLOG.md`** (the
> demolition list). This §7 covers ALL cleanup (incl. consolidate/rename/codify, which are
> not deletions). When an item here becomes a decided deletion, add it to the backlog with
> its scope + KEEP caveat.

**Safe-to-remove / dead (verify no test-tool dependency first):**
1. Schmidl-Cox TX: `modulator.cpp` `generatePreamble()` `:551`, `createSchmidlCoxSTS()` `:332`,
   `g_logged_tx_pilots` `:122` — test-tool-only, OFDM_COX is gone. `generateProbe()` `:662`
   likely dead (verify).
2. Schmidl-Cox RX plateau state machine `ofdm_stream_processor.cpp:243` — forced-only; **keep
   the S-C correlation primitives** (`ofdm_sync.cpp`) reused by warm-LTS.
3. Reserved enum stubs: `CodecType::{LDPC_5G,CONVOLUTIONAL,TURBO,POLAR}`; waveform
   OTFS_EQ/OTFS_RAW/MFSK; FrameType DATA_START/CONT/END (or finish file-segment impl).
4. `carrier_ldpc_interleaver` — default-off, auto-on only on masked carriers; most likely
   removable diversity layer (confirm masked-carrier path first).

**Consolidate (duplication, not dead):**
4b. **Frame-airtime closed form — DONE 2026-07-28, 2→1.** `OFDMChirpWaveform::getMinSamplesForCWCount`
   now DELEGATES to `ofdm_link_adaptation::minSamplesForCWCountExplicit` (the pure query the
   decoder needs to size a frame for a geometry it has not configured yet). The two paths are
   provably one implementation and cannot drift. **KEEP:** `getSamplesPerSymbol()` must stay the
   caller-supplied input — its modulator value (1120 at the GUI default) disagrees with both the
   config-derived fallback (1280) and `connection_policy::kWideOFDMSymbolSamples` (1152), and only
   the modulator's is correct for slicing. Do NOT "simplify" it into the query.
4c. **15 s burst-cadence RTO gap — DONE 2026-07-28, 2→1.** The D2 provisional-HARQ gate's local
   `kTimeoutBatchGapSamples` and the commanded-geometry cadence guard now share
   `streaming_decode_policy::kBurstCadenceRtoGapSamples`. Justified in-comment (steady-state
   groups ≤ ~11.5 s vs ack-RTO ≥ ~19 s; rig-measured 7.00-13.33 vs 19.43/20.01, disjoint).
5. Rate→K table — 5 copies (§3). One source.
6. Entry-SNR floor table (10/12/14/18) — 2 copies: `waveform_selection.hpp:458` vs
   `connection_policy.hpp:212`.
7. `ULTRA_LDPC_Z` — **DONE 2026-05-30, 16→1** (`docs/LDPC_Z_DERIVATION_DESIGN_2026_05_30.md`).
   Z is code-derived via `Connection::selectBurstLiftingZ()`, written to the BURST_HEADER descriptor,
   read back by RX (`activeBurstLiftingZ()`); the app/cli push it to the TX encoder. **NOTE (Transport
   Merge 2026-06-06):** the unified file path uses **z=27** (n=648) — the z=81 long-LDPC branch was the
   legacy `burst_transport_` file behavior and is now dead (`selectBurstLiftingZ` gates z=81 on
   `!kUnifiedSeqEnabled()`, always false → always returns 27; z=27 is also the descriptor's
   "regular frames, not a separate file-burst" signal). Dropping z=81 long-LDPC is a known merge
   trade-off (fewer FEC margin for fade diversity); revisit if file FER on fading regresses. The 1 remaining read is
   the discovery override inside the policy. No-regression proven (cli short-msg + default
   `--file` at Z=27). **REMAINING: faithful GUI proof of the Z=81 burst flip** (use_burst=1) +
   Codex — cli's burst harness is "not the faithful gate".
7b. **`have_burst_descriptor_` stale-TRUE leaks (FOUND 2026-07-28, NOT fixed — orthogonal).**
   Three paths bypass the normal clear and leave the latch TRUE: the `accumulateBurstFrames`
   hard-failure abort and the two deinterleave-throw early returns
   (`streaming_burst_interleave.cpp`); and the group-timeout clear sits inside an
   `if (burst_transport_rx_ && burst_group_callback_)` gate. Effect: they SUPPRESS the
   `ULTRA_COMMANDED_GEOMETRY` guard (which requires `!have_burst_descriptor_`), so a rig run may
   show fewer commanded arms than missed descriptors. Fail-safe (suppression = pre-fix
   behaviour), cheap to fix, but it changes burst-abort semantics so it was deliberately kept
   out of the BUG-BURST-STALE-GEOMETRY change.
8. Rate picker (`selectOFDMCodeRate`/`recommendDataMode`) — the "4 gate arrays × 3 passes"
   over-complexity is real (4 gate arrays/descriptor + QPSK/QAM16/D8PSK/DQPSK passes +
   bootstrap cap + adaptive climb). Per-channel hardcoded D8PSK `if`s `waveform_selection.hpp:612`
   are un-generalized special-cases. Simplify after 3000 (per memory).

**Rename (the adaptivity "tell"):**
9. `dd_qam16_*` on the shared coherent-DD path — functionally generalized to QAM8/QAM16, still
   16QAM-named (`channel_equalizer_equalize.cpp:648`, `channel_equalizer_pilot.cpp:940`).

**Codify (env scaffolding → code-derived, per ADAPT bucket):**
9b. `ULTRA_RETX_TROUGH_PACING` / `ULTRA_TROUGH_DEFER_TC_FRAC` / `ULTRA_COLLAPSE_ESCAPE_ROUNDS` —
    2026-07-03 A/B scaffolding for the retx trough-pacing lever
    (`docs/RETX_PACING_DESIGN_2026_07_03.md`; UNVALIDATED edits-only at landing). After the §5.2
    A/B matrix: graduate (flip defaults ON, keep frac as the one tunable or drop it) or delete the
    whole mechanism. The deferral itself is already channel-derived (Tc from measured/design
    Doppler), so graduation is a default flip, not a codify rewrite. KEEP either way: the
    `executeEscapeDrop` refactor (shared escape action) and the `lastAckProgressFrames` ARQ
    accessor + `setChannelCoherence(score, hz, valid)` doppler plumb (general-purpose observability).
9c. `ULTRA_BELOW_WINDOW_FILE_SALVAGE` — 2026-07-03 interim receiver-side mitigation for
    BUG-ARQ-SEQ-COLLISION (rate-change abort under one-way ACK loss destroys regridded FILE
    resends at the seq-keyed dedup). Labeled prototype by design: the principled fix is the
    move-epoch on DATA frames echoed in ACKs — now IMPLEMENTED knob-gated as
    `ULTRA_ARQ_MOVE_EPOCH` (9d), rig-validation-pending. After the epoch fix validates:
    either keep salvage default-ON as defense-in-depth UNDER the structural fix (it still
    covers pre-epoch peers and the mod-4 wrap blind spot), or delete once the move-epoch
    makes below-window new-content frames impossible. KEEP either way: the file layer's
    straddle-merge/overlap-drain (it serves the legitimate same-epoch regrid resend path).
9d. `ULTRA_ARQ_MOVE_EPOCH` — 2026-07-03 STRUCTURAL fix for BUG-ARQ-SEQ-COLLISION (see the
    §6 row), edits-only/UNVALIDATED, default-OFF byte-identical. Graduation path: rig-validate
    with BOTH stations knob-ON (lockstep — wire/semantics-breaking when ON), then flip
    default-ON and decide the salvage knob's fate (9c). Residuals documented in KNOWN_BUGS:
    mod-4 epoch wrap (4 blind aborts), tone-ACK epoch outside the CRC (Hamming-only,
    fails safe), GROUP_ACK/NACK legacy control frames carry no epoch (retransmit-only,
    never retirement). KEEP either way: the `stale_epoch_acks_ignored` stat and the
    `epochFromFlags`/`epochToFlags` helpers if the wire bits stay.
9e. `ULTRA_DESCRIPTOR_MODE_SWITCH` — 2026-07-03 Phase 1 of the descriptor-committed mode
    switch (docs/MODE_SWITCH_PIGGYBACK_DESIGN_2026_07_03.md, §6 row), edits-only/UNVALIDATED,
    default-OFF byte-identical. Graduation path: §7.2 paired faithful-gate sweep (knob-OFF/ON,
    Good@20 ×5 seeds + Moderate@18 ×3) then the §7.3 rig campaign (W4 reproduction: MODE_CHANGE
    receptions per switch → 0). Phase 2 (escape-path commits via move-epoch + `ULTRA_RX_RATE_CMD`
    tone-ACK steering bits 42-43) is HARD-GATED on 9d rig-validating first. If Phase 1 graduates
    default-ON, log a REMOVAL_BACKLOG candidate for the wideband-ladder MODE_CHANGE retry
    machinery ONLY after the §6.5 deaf-peer fallback proves unneeded (the frame type + handler
    stay for connect-time/USER_REQUEST/MC-DPSK/narrow regardless).
9f. `ULTRA_RX_RATE_AUTHORITY` — 2026-07-05 🟢 **DEFAULT-ON** (campaign flip, same day; `=0` opts out). Receiver-commanded
| `ULTRA_MAX_BURST_AIRTIME_MS` | 🟢 ACTIVE (base 8600, clamp [5000,12000]) | connection.cpp burstAirtimeBudgetFrames | BASE burst ceiling; the streak-gated escalation (2 clean rounds → 11,500 ms → N≈8) is code-constant kEscalatedBurstAirtimeMs (GROUP_SIZE_LEVER 2026-07-07) |
| `shouldUseWideOFDMShortReanchor` (fn, not knob) | 🔴 DEAD (constant false since 2026-07-07) | connection_policy.hpp | Phantom 100 ms/frame charge for the May-removed encoder re-anchor — retired; delete with R11 (REMOVAL_BACKLOG) |
| `ULTRA_SACK_SALVAGE` | 🟡 EXPERIMENTAL (default OFF) | connection.cpp SackedFrameDiscardedCallback | Sender-side skip of re-sending SACKed FILE ranges across rate aborts — DEFUSED after F181 (phantom SACK mark stranded a file); re-enable only after the era-gate chain is root-caused |
| `ULTRA_ZF_LLR_UNBIAS` | 🟡 EXPERIMENTAL (default OFF) | channel_equalizer_equalize.cpp (end of equalize()) | Stage A ZF-consistent LLR unbias — measured MARGINAL on the failing sim cell (docs/LLR_NOTCH_CALIBRATION_2026_07_06.md §measured); controls exactly invariant (QPSK/AWGN); keep OFF unless rig evidence diverges from the sim cell |
| `ULTRA_NOTCH_NV` | 🟡 EXPERIMENTAL (default OFF) | channel_equalizer_equalize.cpp nv denominator (+ raw-obs writer channel_equalizer_pilot.cpp) | Stage B per-carrier reliability floor from raw LS pilot observations — same experiment, same marginal verdict; keep OFF |
| `ULTRA_RX_PREDICTIVE_CLIMB` | 🟢 ACTIVE (default ON, `=0` opt-out) | connection.cpp updateRxAuthorityCommand | EESM per-carrier virtual rung evaluation — direct multi-rung climbs from measured gamma snapshots (docs/RX_AUTHORITY_PREDICTIVE_2026_07_07.md); fallback = one-rung ladder |
| `ULTRA_CHEAP_REANCHOR` | 🟡 EXPERIMENTAL (A/B knob, default OFF ⇒ byte-identical; 2026-07-21, CHANGELOG). Makes the crater re-anchor CHEAP: a full dual-chirp (~1.2s) re-acquires timing+CFO, but a fade only threatens the CFO (warm timing survives). On a crater: roll CFO back to last-CERTIFIED value (`CFOTracker::certifyWarm` snapshots it on delivered groups, `rollbackToCertified()` restores — un-poisons a fade-walked CFO free), keep warm timing, and fire the full chirp only PERIODICALLY (every 4 craters) as a probe. Drops chirps ~1/crater → ~1/4. Attacks the 8.4 re-anchors/run (F520-531) the EMA-hold feeds by holding a high rung through fades. Extends lever #3 (`ULTRA_CRATER_REANCHOR_HOLD`). Receiver-local. | **OFF** | `streaming_ofdm_decode.cpp` decodeCurrentFrame (`kCheapReanchor`, `force_chirp` periodic); `cfo_tracker.hpp` `certifyWarm`/`rollbackToCertified`/`certified_cfo_` | FEAT (A/B) |
| `ULTRA_CRATER_REANCHOR_HOLD` | 🟡 EXPERIMENTAL (**default-OFF, reverted 2026-07-23**, `=1` opt-in; briefly default-on 2026-07-21 (F470-481 re-anchors halved) but the aggregate reckoning F580-591 was a WASH + over-holds failing rungs; throughput-ceiling lever #3. CHANGELOG). TWO-CRATER discipline for the SYNC re-anchor: the RX forces a full chirp+LTS re-anchor (`resetFrameArrivalTrackingLocked` + `expect_full_ofdm_anchor_=true`) on EVERY 0-CW group crater (`streaming_ofdm_decode.cpp` decodeCurrentFrame, ~7×/50KB transfer), treating a deep fade (an AMPLITUDE event — warm TIMING survives) as a lost sync → delayed ACK + ~1.2s chirp on the next group → the operator's "long ACK → RTO → resend" loop. ON: hold warm sync through crater #1 (RX tries warm light-sync next group), re-anchor only on crater #2 — the same restraint the RATE controller's F122 two-crater rule already uses. Safe: `expect_full_ofdm_anchor_` is RECEIVER-LOCAL (own search mode, not a sender signal); worst case = 1 extra warm-attempt group, crater #2 re-anchors + re-centers CFO as before. `crater_reanchor_streak_` resets on any decoded frame. | **OFF** | `streaming_ofdm_decode.cpp` decodeCurrentFrame (`crater_reanchor_streak_` + `ULTRA_CRATER_REANCHOR_HOLD` gate); member `streaming_decoder.hpp` | FEAT (A/B) |
| `ULTRA_DENSE_FAST_DEMOTE` | 🟡 EXPERIMENTAL (A/B knob, default OFF ⇒ byte-identical; 2026-07-23, CHANGELOG). The RX-authority 16QAM OVER-COMMIT fix — the real throughput cap. The predictive climb jumps AGGRESSIVELY into 16QAM (idx 3→8 on 2.5 dB EESM margin over crest snapshots) but the demote is SLOW (F122 two-crater rule) → 82s crater stalls when the jump is wrong. ON: on a dense-mod rung (getBitsPerSymbol≥4), a FULL crater (frame_mask==0) demotes on crater #1 instead of two. Modulation-adaptive (the two-crater grace is for robust rungs); partial 16QAM keeps the grace (per-frame SACK resends cheap); penalty ratchet still gates re-climb (no oscillation); CLIMB untouched. `full_crater` arg threaded through `updateRxAuthorityCommand`. | **OFF** | `connection.cpp` `denseFastDemoteEnabled()` / `updateRxAuthorityCommand` (crater_threshold); test `test_rx_authority.cpp` `test_dense_fast_demote_full_crater` | FEAT (A/B) |
| `ULTRA_BURST_ESC_STREAK` | 🟡 EXPERIMENTAL (A/B knob, value = required clean-group streak 2..8; unset/0 = legacy ⇒ byte-identical; 2026-07-24, CHANGELOG). Repairs an architectural conflict the EVM demote introduced. The burst airtime ceiling escalates 8600→11500 ms (N=5→N=8 per key-down, +19% cycle efficiency) only when `dense_rung` (≥ QAM8 R2/3) + 2 clean groups; `dense_rung` is a PROXY for "the receiver rates this channel calm" (anchor columns encode fading class ⇒ Moderate lives at QPSK). `ULTRA_EVM_DEMOTE` BREAKS that proxy by correctly holding the rung at QPSK when HARDWARE loss (not roughness) depresses usable SNR — rig A6 MPG@20: 100% of 19 fading verdicts were GOOD class (0.28-0.40 vs boundary 0.76) yet 14 of 17 groups were pinned to N=5. ON: a NON-dense rung earns the escalation with a LONGER proven clean-group streak instead. The sender CANNOT use its own fading here (`fading_index_` freezes between sparse control decodes mid-transfer — see `wireFadingIndex()`, rig W5b/W8 pinned 300+ s), so the clean streak (full retire, no holes) is the only fresh sender-side evidence. Self-correcting: any holey/zero-progress round resets the streak ⇒ a Moderate channel never escalates (honors the 2026-07-07 moderate@16 FAIL). Sim vs A6 outcomes: +6.5% @streak2, +4.9% @3, +3.3% @4. **Before default-on: interleaved A/B + a Moderate@16 s42 counter-run + a DUTY governor (A6 measured 94.4% key-down; this lever ADDS key-down).** | **OFF** | `connection.cpp` `burstAirtimeBudgetFrames` (`kNonDenseEscalationStreak` / `streak_proven`); test `test_rx_authority.cpp` `test_group_size_gate_streak` | FEAT (A/B) |
| `ULTRA_ENTRY_EVM_CAP` | 🟡 EXPERIMENTAL (A/B knob, default OFF ⇒ byte-identical; 2026-07-25, CHANGELOG). Caps the CONNECT-time entry rung by the USABLE-domain EVM floors (`evmUsableFloorDbForRung`) using the connect data-aided SNR — itself a usable-domain measurement (MC-DPSK differential EVM), so no dial conversion and no bench constant. Rig MPM@20: an honest 8.3 dB usable reading entered at QPSK R3/4 (needs ~12.5 usable) because `connectSelectionSnrDb`→`dialEquivalentSnrDb` renders it as a ~17 dB dial-equivalent and the physical cap did not bite (physical meter also 17.7 — the ~9 dB hardware-loss gap) ⇒ five straight cratered groups and ~136 s before the ladder reached R1/4. CAP-ONLY + data-aided-only (a training snapshot fade-crest OVER-reads, so it must never license a higher rung). | **OFF** | `connection_handlers.cpp` (ENTRY-EVM cap block); `waveform_selection.hpp` `entryEvmCapEnabled()` | FEAT (A/B) |
| `ULTRA_LINEAR_SNR_RING` | 🟡 EXPERIMENTAL (default OFF ⇒ byte-identical; 2026-07-26, CHANGELOG). Fixes BUG-LADDER-SNR-DB-MEAN: `connection.cpp` summed **dB** for the rate-ladder SNR ring while `streaming_decoder.hpp::physicalSnrStats` correctly sums **linear power** (and documents it), against a dial and anchor table that are BOTH mean-power definitions ⇒ Jensen makes the ladder read LOW on fading. **The knob switches the ring domain AND pulls the anchor offset down by `kLinearRingJensenCompensationDb = 1.70` in ONE switch, so the halves cannot be separated** — fixing the domain alone RAISES the ladder input on a path already measured to over-commit ~2 rungs. Compensation MEASURED on the ring's own per-group samples (sd 4.36 → gap 1.65 dB), NOT on per-frame readings (sd 5.60 → 2.87 dB) which would over-correct by >1 dB; an independent audit got 1.74 the other way. **RIG A/B = WASH** (4 pairs, paired −2.5%, p=0.688; churn 6.2 vs 6.8/run; completion 3/4 vs 4/4) but sizing CONFIRMED neutral (matched pair snr_avg 23.7 ON vs 23.6 OFF). Stays OFF: a correct change with no measurable benefit is not enabled on theory. Kept because the **anchor re-measure must run in the correct domain**. An explicit `ULTRA_OFDM_ANCHOR_OFFSET_DB` still overrides. | **OFF** | `connection.cpp` ring in `updateRxAuthorityCommand`; `connection_policy.hpp` `linearSnrRingEnabled()`/`kLinearRingJensenCompensationDb`/`ofdmAnchorScaleOffsetDb()`; test `test_connection_policy.cpp` `test_linear_ring_couples_offset_compensation` | FEAT (A/B: wash) |
| `ULTRA_OFDM_ANCHOR_OFFSET_DB` | 🟡 EXPERIMENTAL / MEASUREMENT KNOB (default = `kOfdmLegacyAnchorScaleOffsetDb` 8.70 ⇒ byte-identical; 2026-07-26, CHANGELOG). Overrides the marking offset for the two RATE-RELEVANT consumers (ladder input in `modem_protocol_binding`, EESM predictive-climb scale in `streaming_burst_interleave`); the DISPLAY consumer is deliberately untouched. Exists to MEASURE the hardware over-commit, NOT to become a second tuned constant. **RIG RESULT (8 interleaved runs, 3.1 vs 8.70): the over-commit hypothesis is CONFIRMED and the value is BRACKETED.** At 3.1: rung commands **2.8 vs 8.0/run (−65%)**, craters **0.50 vs 1.25 (−60%)**, completion **4/4 vs 3/4**, and `raw ≈ cmd` (1.9 vs 2.3) so NO clamp fires — whereas at 8.70 raw 6.2 → cmd 5.3 means the crater machinery continuously hauls the ladder off a rung it cannot hold. That IS the crater/churn cycle. BUT 3.1 OVER-corrects: commanded rung 2.3 vs 5.3 and completed-run goodput −5.3%, so part of the 5.6 dB residual is the implementation loss the two-SNR model says SHOULD separate channel from usable SNR. True value lies in (3.1, 8.70); finding it by A/B sweep would be fitting a BENCH CONSTANT (forbidden by the ADAPTIVITY rule and non-transferable) — resolve via the **§3 anchor re-measure** then DELETE the offset. Parsed with `strtod` + full-string match (`atof` would turn a typo into 0.0, a legitimate value here, silently un-calibrating the ladder) plus a ±20 dB sanity range. | **8.70 (default unchanged)** | `connection_policy.hpp` `ofdmAnchorScaleOffsetDb()`; consumers `modem_protocol_binding.hpp`, `streaming_burst_interleave.cpp`; test `test_connection_policy.cpp` `test_anchor_offset_override` | FEAT (measurement) |
| `ULTRA_ANCHOR_SKIP_KEEP_STREAK_ON_SWITCH` | 🔴 **MEASURED HARMFUL — default-OFF PERMANENTLY** (2026-07-26, CHANGELOG). Preserved the #69 anchor-skip clean streak across a descriptor mode/rate switch, arguing the streak is DELIVERY evidence and a config switch says nothing about whether the channel still syncs chirp-less. Rig A/B (sender-side knob, env verified via /proc/PID/environ, stopped after 2 pairs on the pre-committed falsifier): chirp-skip rate FELL **28.9% → 7.7%**, mean anchor prefix ROSE **1.221 → 1.733 s**, craters **1.0 → 2.0/run**, resends **0.0 → 1.0/run**, max streak 14 → 8. ROOT CAUSE: the recool is doing DOUBLE DUTY — besides delivery evidence it is a **POST-SWITCH ACQUISITION GUARD**. Preserving the streak lets the skip fire 1 burst after a geometry change (vs 4-5 with the recool) = the riskiest moment to omit a chirp, since the RX must re-acquire on a new constellation; those groups crater, the crater forces a resend, and the resend recools the streak anyway ⇒ MORE craters AND LESS skipping. Re-confirms 2026-06-17 (short anchors: ON 1.157 vs OFF 1.187 kbps) from a new angle. Any retry MUST add an explicit post-switch skip holdoff. **KEEP** the `anchor_reason` plumbing (None/Resend/ModeSwitch) — it is a clarity win and is what made this measurable. | **OFF (permanent)** | `streaming_encoder.cpp` anchor-skip gate + `anchor_full_keep_streak_once_`; `streaming_encoder.hpp` `forceNextBurstGroupStartFullPreambleKeepStreak()`; `modem_engine.hpp` `forceNextBurstFullPreambleKeepSkipStreak()`; `connection.hpp` `kAnchorReason*`; test `test_connection_adaptive.cpp` reason-plumbing CHECK | FEAT (rejected) |
| `ULTRA_LATENT_RATE` | 🟢 **ACTIVE — DEFAULT-ON since 2026-08-01 (v0.5.1-pre-alpha)**, opt out `=0` ⇒ legacy SNR-anchor ladder + corrective stack. THE production OFDM rate controller. Fits a latent operating point `x` from OUTCOMES (frames SACKed per group) against a link model measured as FER-vs-SNR (`docs/FADING_ANCHOR_MEASUREMENT_2026_07_26.md` §1), so every group updates the predicted success of ALL rungs at once and evidence SURVIVES a rung change (re-convergence 0 groups, against 4-8 for the predecessor). **CONSUMES NO SNR ESTIMATE** — with `P_r = f(x − theta_r)` and `x` fitted from outcomes, adding a constant to every `theta_r` is absorbed by `x`, so COMMON-MODE CALIBRATION ERROR CANCELS EXACTLY; that is what let `kOfdmLegacyAnchorScaleOffsetDb` leave the rate path. No dwell, no ratchet, no penalty array, no crater trigger (Minstrel's discipline: stability in the ESTIMATOR, not the ACTUATOR). Candidate value now uses the actual logical→physical CW/Z file profile and production payload/timing helpers. A non-incumbent pays the descriptor-switch full-anchor cost in both first-cycle N and airtime; every row uses the guaranteed base ceiling rather than receiver-local clean callbacks that may correspond to lost ACKs. **RIG: +14.5% mean, median +18.4%, 7/8 positive, 1.64 vs 1.45 kbps, paired t=2.92 df=7 → p=0.022** — the only significant throughput result of the 2026-07/08 campaign. The gain is STABILITY not rung selection: it reaches 8PSK R2/3 LESS than the legacy ladder (0.4 vs 1.2 changes/run) and wins anyway on 2.2 vs 5.0 mode changes. Moderate VALIDATED (posterior settles x~11-12 vs 15-17 on Good, holds QPSK R1/2-R2/3) even though `theta_r` was measured on Good only. **Z81 CAVEAT:** geometry is now exact, but `theta_r` is still logical-rung/Z27-calibrated; automatic long-profile choice remains experimental until profile-specific FER curves exist. Long-profile env parity is a campaign precondition, not negotiated peer state. **Statistical caveat: significant by t-test, NOT by sign test (7/8, p=0.070) — established, not settled.** | **ON** | `latent_rate_controller.hpp`; early return and candidate geometry in `connection.cpp::updateRxAuthorityCommand`; tests `test_latent_rate_controller.cpp` (50 checks), `test_connection_adaptive.cpp` | FEAT |
| `ULTRA_LATENT_RELAX_DB` | 🟡 EXPERIMENTAL (float dB/group, 0..5, default **0.35**). How fast the latent posterior forgets — the controller's ONE free parameter. Too small freezes it on a rung that stopped being right, which would look EXCELLENT on OTASim (stationary Watterson, NO ionospheric time-variation) and fail on the rig; too large and the posterior never sharpens, `argmax` returns the prior, and the controller reduces to the anchor table. Rig-checked at the default: posterior stayed live (sd 1.4-2.1, tracking k=3/5..5/5), so the freeze mode did NOT fire. Not swept. | **0.35** | `latent_rate_controller.hpp::relax()`, read in `connection.cpp` latent path | FEAT |
| `ULTRA_TRUST_LADDER_PICK` | 🟡 EXPERIMENTAL (`=1`; default OFF). Commands the ladder's RAW pick, bypassing the corrective stack, retaining only `snapRungIndexDownToEnabled` (F145 deadlock guard) and a catastrophic-crater escape. Built on the measurement that the SELECTOR IS NOT THE DEFECT: over 135 rig verdicts the ladder picks 8PSK R2/3 **66%** of the time but only **26%** reaches the wire — 46% of decisions clamped down a mean of 1.61 rungs. RIG +20.7% (6 pairs, p=0.16, NOT significant). Largely SUPERSEDED by `ULTRA_LATENT_RATE`, which returns before this code. | **OFF** | `connection.cpp::updateRxAuthorityCommand` | FEAT (A/B) |
| `ULTRA_RUNG_DWELL_MS` | 🔴 **MEASURED HARMFUL — do not enable** (ms, 0=off, default **0**). Minimum time between rung changes. Built on correlation(mode changes, goodput) = −0.58 over 15 runs. **RIG FALSIFIED IT: −13.8%, 1/4 positive.** Mode changes dropped 5.5→3.5 exactly as designed and goodput got WORSE — the correlation was REVERSE CAUSATION (a rough channel causes both the churn and the low goodput), and the changes are mostly CLAMPS firing rather than the ladder re-deciding, so throttling merely froze whatever a clamp had dragged us to. Kept behind the knob and documented so it is not re-proposed. | **0 (off)** | `connection.cpp::updateRxAuthorityCommand` | FEAT (falsified) |
| `ULTRA_INFLIGHT_RTO` | 🟡 EXPERIMENTAL (`=1`; default OFF). Sizes the ARQ retransmit timeout on frames ACTUALLY outstanding rather than the window maximum. Measured curve (8PSK R2/3 cw=12): n=1 → 8.0 s, n=5 → 17.5 s, n=8 → 25.8 s, n=16 → 47.7 s, so a 1-frame tail waits 8 s instead of 25-48. The Connection supplies a table from the SAME policy function, so index [window] reproduces the legacy scalar exactly (identity logged + CTest-asserted). RIG INCONCLUSIVE: +65%/−19% over two valid pairs. | **OFF** | `selective_repeat_arq_policy.hpp::inflightRtoEnabled`, table built in `connection.cpp` | FEAT (A/B) |
| `ULTRA_ADAPTIVE_RTO` | 🟡 EXPERIMENTAL — **hypothesis RETRACTED** (`=1`; default OFF). Stops `configured_ack_timeout_ms` being used as BOTH floor and ceiling of the RFC6298 estimator (the clamp collapses to a point and the estimate is discarded every round trip). The throughput claim that motivated it was WRONG: built from a 44.7 s timeout read off the RECEIVER's log, but the SENDING path's value is 17.4-23.1 s, and null-controlled rig measurement (53 confirmed firings) shows the adaptive floor moves the RTO by only **0-20%**, not 7.5x, because measured srtt is ~11-14 s. First paired result −25%. | **OFF** | `selective_repeat_arq_policy.hpp::adaptiveRtoEnabled` | FEAT (A/B) |
| ~~`ULTRA_CONNECT_ACK_RESCUE_DEFER`~~ | **REMOVED 2026-08-03 — defect closed without the falsified timer.** An accepted OFDM sync is not handshake proof (rig: weak sync on a duplicate MC-DPSK CONNECT, followed by 0/6), so it no longer clears the cached CONNECT_ACK or confirms the responder. The responder never arms a proactive full-ACK timer: it keeps only a cached ACK for collision-safe reactive replay. While confirmation is pending, the connected wide-OFDM (`OFDM_CHIRP`) decoder retains only a true expected dual-chirp anchor and, once the complete raw span is resident, makes one fixed DBPSK R1/4 MC-DPSK CONNECT attempt; weak LTS/data fallbacks never arm it. A recovered duplicate CONNECT silently aborts the false OFDM group, restores the pre-half-open SNR/EVM/fading baseline, re-arms the genuine OFDM anchor, and immediately replays the retained ACK after the initiator body ends. A CRC-valid post-CONNECT frame addressed to us and sourced by the established peer clears the cache and confirms. Empty/all-failed groups and other-station frames do neither. `OFDM_NARROW` is not part of this raw-ring cross-mode fallback. | **gone** | `connection.cpp` accepted-sync/authoritative-confirm/duplicate-CONNECT paths; `streaming_sync_acquisition.cpp` candidate arm; `streaming_decoder.cpp` bounded raw-ring fallback; focused connection + streaming loopback regressions | CORRECTNESS |
| `ULTRA_TXLAT_DIAG` | 🟡 DIAGNOSTIC (unset = inert). Timestamps sender-clock turnaround boundaries: `ack_heard` (`connection.cpp::onToneBurstAck`), `audio_ready` + sample count (`modem_engine.cpp::postProcessTx`), `queue_submit` + queue depth and `first_out` (`audio_engine.cpp`). Logging `n` is the point — burst airtime becomes MEASURED rather than modelled from `data_ms`, and that modelling error is what inflated the earlier turnaround figure to 31.9% when the true value is 16-19%. Produced: encode+ARQ is 62-89 ms (killing the pre-encode lever), ACK submit→air spread = the audio callback period (177 ms at 8192, 52 ms at 2048), and 0-of-34 sender submissions found a non-empty TX queue (killing the flush hypothesis). | **unset** | `connection.cpp`, `modem_engine.cpp`, `audio_engine.cpp` | DIAG |
| `ULTRA_AUDIO_BUFFER` | 🟡 EXPERIMENTAL (samples/callback, 64..16384; default **8192 unchanged**). SDL period for BOTH input and output. The in-code claim "latency cost is negligible — a frame is 660 ms+ of audio anyway" is MEASURED FALSE: on a half-duplex link the buffer is crossed FOUR times per turnaround and turnaround is 16-19% of wall clock. Measured ACK submit→air spread 177 ms at 8192 vs **52 ms at 2048** (3.4x, mechanism confirmed). Default deliberately unchanged — the same comment records real Pi 5 + C-Media USB-1.1 XRUNs at 1024-4096, which is hardware evidence; macOS/CoreAudio is the safe end to test. Goodput not resolved (rig variance). | **8192** | `audio_engine.hpp::buffer_size_`, consumed at both `SDL_OpenAudioDevice` sites | FEAT (A/B) |
| `ULTRA_GOODPUT_RATE` | 🟡 EXPERIMENTAL (A/B knob, `=1` enables; default OFF ⇒ byte-identical; 2026-07-30, CHANGELOG). **REPLACES** the SNR-anchor pick + its ~10 correctives in `updateRxAuthorityCommand` with a goodput-maximizing controller, returning early rather than composing (layering would keep the churn it exists to remove). Motivated by a rig measurement: forced 8PSK R2/3 beat the auto ladder **+10.8%/+10.4% on both pairs** of an interleaved MPG@20 A/B (2.06 vs 1.86, 1.59 vs 1.44) while making ONE mode change against 5 and 8 — the pinned arm never demoted even on the rougher epoch, so every demote the ladder made was unnecessary. Sender-clock accounting the same day showed airtime is 81-84% of wall and runs at 90-99% of the rung's OWN raw ceiling, i.e. the loss is rung SELECTION, not wasted airtime. Decides on MEASURED delivered goodput over a coherence-derived window (`Tc = 0.423/f_d` shared with `kClarkeCoherenceNumerator`, 8 independent fades ⇒ 4 groups at MPG) and **reads NO SNR** — deliberate, since the anchor/scale chain is this project's largest source of wrong rate decisions. HOLD is a theorem (`f·eta_cur ≥ eta_below` ⇒ no lower rung can win, since `f_below ≤ 1`); DEMOTE requires falling below `break_even/(1+switch_margin)`; CLIMB is an explicit bet judged after 2 groups against the measured goodput of the rung it came from, with exponential per-rung backoff. **Three defects found on the first rig attempt and fixed**: switch-cost sign inverted (`× (1+m)` instead of `÷`, which demoted rungs that were WINNING — logged `f_window=0.800 break_even=0.750 reason=goodput-demote`, walked R3/4→R2/3→R1/2, 1.38 kbps); backoff written to the climb TARGET but read from the CURRENT rung (so a failed probe placed no obstacle at all — the SAME `idx 4 -> 5` climb three times, 9 mode changes, 1.25 kbps); and the window resized mid-fill, aliasing the ring (`n=2/3`). **A/B post-fix PENDING** — the only pre-fix paired result was 2.01 vs 1.53 (+31%) with 3 mode changes vs 6. NULL CONTROL: `gp_decisions` counts GOODPUT-RATE log lines; zero on a GOODPUT arm means the knob never engaged. | **OFF** | `goodput_rate_controller.hpp` (`GoodputRateController`, `windowGroupsForCoherence`, `snapRungIndexUpToEnabled`); `connection.cpp` early-return in `updateRxAuthorityCommand`; test `test_goodput_rate_controller.cpp` (71 checks) | FEAT (A/B) |
| `ULTRA_ADAPTIVE_RTO` | 🟡 EXPERIMENTAL — **measuring wash-to-harmful, hypothesis RETRACTED** (`=1` enables; default OFF ⇒ byte-identical; 2026-07-30, CHANGELOG). `selective_repeat_arq_policy.hpp::updateRTO` implements a correct RFC6298 estimator and then uses `configured_ack_timeout_ms` as BOTH floor and ceiling, so `clamp()` collapses to a point and the estimate is discarded every round trip. ON: the configured value stays as initial value and CEILING, the srtt-derived floor governs below it. **The throughput claim that motivated it was WRONG and is withdrawn.** It was built from a 44.7 s timeout read off the RECEIVER's log — the Mac is not the file sender; the SENDING path's configured value is 17.4-23.1 s. Null-controlled rig run (`ADAPTIVE-RTO ENGAGED` logging, sender engagement CONFIRMED at 53 firings) shows the adaptive floor moves the RTO by only **0-20%**, not 7.5x, because measured `srtt` is ~11-14 s so `srtt + 4·rttvar` lands on top of the configured value. The unit test's dramatic 6000-vs-44700 uses a SYNTHETIC 2000 ms sample that does not occur on this link. First paired result ADAPTIVE 1.12 vs BASELINE 1.50 (**-25%**). The underlying observations remain true (the estimator IS clamped away; the configured value IS sized on a full window rather than frames in flight) but they are not worth the throughput claimed. **Do not default-on without a fresh, null-controlled multi-pair A/B.** | **OFF** | `selective_repeat_arq_policy.hpp` `adaptiveRtoEnabled()`/`updateRTO(..., adaptive_floor)`; `selective_repeat_arq.cpp` `maybeSampleRTT` (+ ENGAGED null-control log); test `test_selective_repeat_policy.cpp` (88 checks, both paths pinned) | FEAT (A/B) |
| `ULTRA_AUDIO_BUFFER` | 🟡 EXPERIMENTAL (samples per SDL callback, clamped 64..16384; unset ⇒ **8192 unchanged**; 2026-07-30, CHANGELOG). Overrides `AudioEngine::buffer_size_`, used for BOTH input and output devices. Exists because the in-code claim "latency cost is negligible for an HF modem — a single OFDM data frame is 660 ms+ of audio anyway" is **MEASURED FALSE**: it compares the buffer to a frame, but on a HALF-DUPLEX link the buffer is crossed **four times per turnaround** (sender TX out → receiver RX in → receiver ACK out → sender RX in) and turnaround is 16-19% of wall clock. Sender-clock decomposition put burst-end→ACK-heard at 1.79-1.91 s of which only ~760 ms is named (lead-in/tail 200 + decode 2 + ACK lead-in 150 + ACK tone 408); the unnamed ~1.1 s matches 4 × 170 ms plus SDL double-buffering. The receiver is NOT slow — "Burst group complete"→"TX ToneBurstAck" measures **2 ms**. Also explains the operator's waterfall observation that the ACK sometimes butts against the OFDM and sometimes trails it by ~2 s: the delay depends where each event lands inside the callback period. **Default deliberately unchanged**: the same code comment records real Pi 5 + C-Media USB-1.1 XRUNs at 1024-4096 (avail_max at 96% of capacity), which is hardware evidence. macOS/CoreAudio is the safe end to test. **NOT YET A/B'd.** | **8192** | `audio_engine.hpp` `buffer_size_` initialiser; consumed at `audio_engine.cpp` `want.samples` for both `SDL_OpenAudioDevice` calls | DIAG/FEAT |
| `ULTRA_TXLAT_DIAG` | 🟡 EXPERIMENTAL — **diagnostic only, NOT in the committed tree** (2026-07-30). Timestamps two sender boundaries (`Connection::onToneBurstAck` entry = ACK heard; `ModemEngine::postProcessTx` entry = audio ready, logging the sample count) so a turnaround can be decomposed on ONE clock. Applied directly to the Pi 5's working tree for the measurement and **must be reverted there**; not present on `main`. Produced the two load-bearing corrections of 2026-07-30: encode+ARQ is **62-89 ms** (so pre-encoding the next burst during the ACK wait is worth 0.85% of wall clock — lever killed before it was built), and measured airtime is 81-84% of wall, retiring the earlier "turnaround = 31.9%" figure which had MODELLED airtime from `data_ms` instead of measuring it. | **unset** | Pi 5 working tree only — `connection.cpp` `onToneBurstAck`, `modem_engine.cpp` `postProcessTx` sites | DIAG |
| `ULTRA_CRATER_GOODPUT_GRADE` | 🟢 ACTIVE (**DEFAULT-ON since 2026-07-26**, opt out `=0` ⇒ legacy binary predicate; 2026-07-25/26, CHANGELOG). Grades the RX-authority CRATER predicate by DELIVERED FRACTION instead of the binary quality byte. The decoder assigns quality EXACTLY 0.0 to any group that is not perfect (`streaming_burst_interleave`), so `q=0.00` means "≥1 frame of N lost", NOT "the rung failed" — and two in a row demote a rung while climbing needs clean groups + cooldowns, so fast-down/slow-up walks a healthy link to the floor. Rig MPG@20 (3 transfers, 64 groups): 21 groups fed crater evidence but only 6 were true 0/N; the other **15 (71%) delivered a MEAN 67%**, including 7/8 (88%) and 4/5 (80%) counted identically to total loss. That is the first-quartile deficit: Q1 mean 1386 bps vs 2434/2826/2384 for Q2-Q4 = **54% of the rest = 19% of wall clock** (run 2 ratcheted idx 4→1 from t=48-127 s while snr_avg ROSE to 22 dB and the ladder's own pick was idx 8, then hit 3111/4842 bps once it climbed back). ON: a lossy group is rung-failure evidence only when its delivered fraction falls below `goodputBreakEvenDeliveredFraction(cur)` = **eta(first enabled rung below)/eta(cur)**, eta = modulation bits × code rate. DERIVED from ladder geometry (airtime ∝ 1/(f·eta) ⇒ larger f·eta wins; the rung below is credited the optimistic f=1, which makes demoting EASIER not harder), so it is correct for the whole rung family by construction with no bench constant, and it snaps through `snapRungIndexDownToEnabled` so the threshold matches the rung actually landed on, not idx-1 which may be a hole (F145). 16QAM R2/3→R1/2 = 0.75; QPSK R3/4→R2/3 = 0.889; floor = 0. A true 0/N crater stays evidence at EVERY rung. **RIG-VALIDATED: 16 interleaved same-epoch runs, 8/arm** — completed **8/8 vs 5/8** (p=0.100), completed-run goodput 1689 vs 1554 bps (**+8.7%**), **pathological demotes 1.57 vs 2.88/run (-45%, the direct causal metric)**, craters 1.0 vs 2.1 (falsifier passed INVERTED), rung changes 5.4 vs 8.0; sign test 6+/1-/1= p=0.0625, median paired +29.8%. No single metric reaches p<0.05 on a ±25%-noise rig, so the case rests on six converging metrics + the principled derivation. Shipped as a CORRECTNESS fix: "7/8 delivered" is not rung-failure evidence regardless of the delta. **Earlier +56% and +30.7% figures were TIMEOUT-DRAW ARTIFACTS, not the effect size** (a timed-out run scores ~830 vs 1380-2170 completed). NULL CONTROL (an ON run with regrades==0 is byte-identical to OFF) landed +2.3% vs OFF = ~0, the internal-validity check. Default pinned by `test_crater_grading_is_default_on`, verified to catch a silent revert (20/21). | **ON** | `connection.cpp` `craterGoodputGradeEnabled()` / regrade in `updateRxAuthorityCommand`; `waveform_selection.hpp` `rungSpectralEfficiency`/`goodputBreakEvenDeliveredFraction`; test `test_rx_authority.cpp` `test_goodput_break_even_is_rate_ratio` | FEAT (A/B) |
| `ULTRA_EVM_DEMOTE` | 🔴 **BLOCKED — default-OFF; a DOUBLE-DRIVER problem, not a tuning problem** (2026-07-24 built, 2026-07-26 measured; CHANGELOG). Stage 2 of the radio-agnostic SNR work — the PRINCIPLED form of the 16QAM over-commit fix. The RX-authority ladder is steered by the OFDM broadband SNR marked up `+kOfdmLegacyAnchorScaleOffsetDb` (8.70) to the legacy-anchor dial scale; on a real radio that offset MASKS the ~5 dB hardware loss (IONOS WGN@20: 14.1→22.8 → commits 16QAM R2/3 the ~15 dB usable channel can't hold). The decision-directed EVM SNR (`demodulator_impl.hpp`, Stage 1) reads USABLE dB directly — constant-free, non-inflating (MC-DPSK's differential-EVM analogue for OFDM). ON: after the command is computed, if this group's usable EVM cannot support the CURRENT rung's EVM-usable floor (`evmUsableFloorDbForRung`, measured OTASim AWGN sweep: QPSK R2/3 9.4·R3/4 12.5·8PSK R2/3 13.7·16QAM R1/2 14.9·R2/3 16.7), clamp DOWN to the highest EVM-supported rung (`highestRungSupportedByEvm`, 1 dB hold margin). Demote-ONLY (EVM saturates at the demod ceiling, never a climb input); pure output clamp, orthogonal to crater/penalty machinery; measurement-backed → lands directly (not bounded by the 2-rung down-limit). Radio-agnostic by construction — the anti-footgun for the +8.70 bench constant. **RIG A/B 2026-07-26: HARMFUL in BOTH forms.** Raw clamp: rung commands **15 vs 7/run** (it DOUBLED the churn it existed to correct), scattering idx 5→4, 3→2, 7→5, 7→3, 3→1 in one transfer. First diagnosis was an SNR-RESOLUTION error — per-group EVM sd **3.1-4.4 dB** vs adjacent rung floor gaps of only **1.2-3.1 dB**, so resolving the tightest boundary needs **N≥27** samples and one sample scatters the target 2-3 rungs. Added `ULTRA_EVM_DEMOTE_CONFIDENT` (`mean + 1.645·se < floor − margin` over a ring; z is a PROBABILITY constant, self-sharpening, still fires at n=1 when unambiguous). That fixed false positives (craters 2.0 → 1.0) but NOT the churn (**17 vs 7.5/run**), because the clamp still yanks **2-5 rungs** per firing. TRUE ROOT CAUSE: the ladder steers on dial-equivalent `snr_avg` while the clamp reads USABLE dB — measured mean disagreement **14.3 dB** (16.4/15.2/13.7/12.0), MORE than the +8.70 offset alone, so real hardware loss sits on top. The clamp is therefore a SECOND DRIVER with an irreconcilable input and its target is ~4-5 rungs below the ladder's BY CONSTRUCTION = the F128 double-driver failure. Gating changes WHEN it fires; the scale gap determines WHERE it points. **BLOCKED on the §3 anchor re-measure + deleting `kOfdmLegacyAnchorScaleOffsetDb`** (docs/SNR_CALIBRATION_HANDOFF_2026_07_08.md) — once the ladder is steered by honest usable dB the EVM stops being a second driver and becomes the ladder's own input. **KEEP** the confidence machinery (correct and reusable for any rung decision fed by a σ≈3 dB estimator). The 2026-07-24 "+21% on 3 pairs" note is **SUPERSEDED** — it did not survive a churn-aware retest. | **OFF** | `connection.cpp` `evmDemoteEnabled()` / `updateRxAuthorityCommand` (EVM clamp + `burst_obs_evm_snr_db_`); `waveform_selection.hpp` `evmUsableFloorDbForRung`/`highestRungSupportedByEvm`/`kEvmDemoteHoldMarginDb`; `demodulator_impl.hpp` EVM accumulators; plumbing `ModemEngine::getLastEvmSnrDb`→`ProtocolEngine`/`Connection::setBurstEvmObservation`→binding; tests `test_rx_authority.cpp` (`test_evm_floor_table_and_mapping`, `test_evm_demote_strips_overcommit`, `test_evm_demote_inert_when_supported`), `EVMSnrCalibration` | FEAT (A/B) |
| `ULTRA_COMMANDED_GEOMETRY` | 🟡 Broad commanded-rung reconstruction is **DEFAULT-OFF since 2026-07-29** (`4055831`). The independent group-start truncation guard is unconditional. As of 2026-08-04, a narrow BI0 correctness path is default-active when a BURST_HEADER is missed but the receiver has already learned the exact long-profile geometry from the wire: same current modulation/rate/codeword count, active Z27, learned Z81, and only 8PSK R2/3 cw4 or QPSK R3/4 cw3. It now covers both provenance shapes: a marked head after descriptor loss, and—after control-first fails—a strong later classic member when the descriptor and marked head were both lost inside an accepted full-anchor epoch. RX authority, correlation >=0.80, not-declined evidence, and the steady-cadence/post-RTO discriminator remain mandatory; cadence is keyed to the accepted anchor rather than the later member. BI0 proves exact `N` only from the CRC-protected physical-tail flag: stale configured `N` is ignored, collection and the half-duplex interlock extend to a sender-policy-derived ceiling, and ceiling/timeout without a tail emits no callback/ACK. The classic fallback likewise synthesizes no descriptor/N/group callback; only a decoded physical tail closes the turn. Learned geometry survives connected echo resets but clears on session teardown. BI1 cannot inspect the tail flag until after its `N`-dependent deinterleave and therefore fails closed when the descriptor is missing. The BI1 boundary was live-falsified by sender N7/stale receiver N8: fabricated air-end was 2.382 s early, the ACK was dropped into inbound audio, and seven frames waited for RTO. Review then caught inverse BI0 N8/stale-N7 before deployment; its exact-tail and corrupt-tail regressions now pin recovery versus fail-close. Broad A/B evidence remains a null control (`cmd_arms=0` in 7/8), with fresh immutable IONOS revalidation still owed. | **OFF for broad reconstruction; exact learned same-rung Z81 recovery ON for BI0 only** | `streaming_decode_policy.hpp` `commandedGeometryEnabled()` + `kBurstCadenceRtoGapSamples`; `streaming_ofdm_decode.cpp` marked/classic resolvers, learner, and truncation guard; `streaming_burst_interleave.cpp` tail-only finalization and policy ceiling; readiness mirror in `streaming_sync_acquisition.cpp`; test `tests/test_burst_stale_geometry.cpp` | FEAT (broad experiment) + correctness (narrow BI0 recovery) |
| `ULTRA_RX_EMA_HOLD` | 🟡 EXPERIMENTAL (**default-OFF, reverted 2026-07-23** `=1` opt-in; briefly default-on 2026-07-21 but the all-levers-ON vs baseline reckoning F580-591 was an aggregate WASH + over-holds failing rungs → 88s stalls; throughput-ceiling lever #1, CHANGELOG). Two coupled corrections to the RX-authority crater response, targeting the fast-vs-slow variance (F344 2.61 vs F372 1.16 kbps SAME channel = rate-controller limit cycle, not PHY): (1) **EMA-supported HOLD** — a confirmed crater does not demote/ratchet while the fade-averaged ring `snr_avg` STRICTLY exceeds the current rung's class floor (`rungClassAnchorDb`, new ladder-aware+class-specific anchor); consecutive craters at a supported average are deep-null brushes the ARQ resends through. (2) **Censored failed-group SNR** — a non-delivered group enters the obs ring right-censored at `min(obs, calibrationAnchorDbFor)` instead of the stale-crest `burst_obs_snr_db_`, killing the survivor bias so sustained real failure drives `snr_avg` below the class anchor and DOES demote (strict `>` breaks the censor==anchor latch). No new magic constants (thresholds are the rung's own anchors). Preserves the F122-F160 crater_streak≥2 / one-rung-down / ratchet / dwell machinery — only makes the demote conditional on the measured average. Unset/`=0` → byte-identical. Read per-group (togglable in tests). | **OFF** | `connection.cpp` `emaHoldEnabled()` / `updateRxAuthorityCommand` (censored ring feed + `ema_supports_cur`/`demote_on_crater`); `waveform_selection.hpp` `rungClassAnchorDb`; tests `test_rx_authority.cpp` (`test_ema_hold_absorbs_supported_crater`, `test_ema_hold_still_demotes_sustained_failure`) | FEAT (A/B) |
    ABSOLUTE rung selection: receiver maps its fresh per-group observation (broadband SNR EMA +
    coherence-adjusted fading, `Connection::setBurstChannelObservation` fed by the modem binding)
    through `selectCoherentOFDM` and stamps the canonical rung index
    (`waveform_selection.hpp kRungIdx*`) into the ACK's reinterpreted `[rate_hint|rung_cmd]`
    bits (5 bits, airtime-neutral); sender obeys via DESC-SWITCH/MODE_CHANGE
    (`maybeObeyAuthorityCommand`, connection.cpp) and its mid-transfer drivers are inert under
    the knob (REMOVAL_BACKLOG R10 lists the post-graduation deletions + the KEEP rails).
    BOTH ends must set it (binds the widened tone-ACK CRC span — knob-OFF peer CRC-rejects,
    fails safe as ack loss). Supersedes `ULTRA_RX_RATE_CMD` when on. Tests:
    `tests/test_rx_authority.cpp`.
9g. **2026-07-05 CAMPAIGN DEFAULT-ON FLIP (the "quazillion knobs" graduation).** The
    validated standing stack became the binary's defaults; every knob below now reads
    DEFAULT-ON (or the listed default value) with `=0` (or an explicit value) opting out.
    Booleans flipped ON: `ULTRA_ACK_SNR_MEDIAN`, `ULTRA_ARQ_MOVE_EPOCH`,
    `ULTRA_CONNECT_AFFINE_BASIS`, `ULTRA_CONNECT_SNR_POOL`, `ULTRA_DESC_ARMED_ACCUM`,
    `ULTRA_DESCRIPTOR_MODE_SWITCH`, `ULTRA_ENABLE_PSK8_LADDER`, `ULTRA_ENABLE_QAM16_LADDER`
    (⇒ the DEFAULT auto ladder is now `kCoherentLadderPsk8Exp` — QAM8 + 16QAM rungs live),
    `ULTRA_ENTRY_CAP_R34`, `ULTRA_ESCAPE_EPISODE_CAP` (value default 1), `ULTRA_LLR_REJECT_SHAPE`,
    `ULTRA_PRESERVE_ANCHOR_WAIT`, `ULTRA_PROMOTE_EMA_CARRY`, `ULTRA_QAM16_CW16`,
    `ULTRA_QAM16_DEMOTE_MIDRUNG`, `ULTRA_QAM16_R34`, `ULTRA_R34_FAST_CREST`,
    `ULTRA_RETX_TROUGH_PACING`, `ULTRA_RX_RATE_AUTHORITY` (9f), `ULTRA_RX_RATE_CMD`
    (voice-only under authority), `ULTRA_TROUGH_AMNESTY`, `ULTRA_EARLY_FRAME_DECODE`
    (born default-ON), `ULTRA_ACK_CCA_DEFER_MS` (born default 2500).
    Value defaults changed in that campaign: `ULTRA_ACK_REPEAT_SILENT_MS`
    0→4000 (**later reverted; current production default is OFF and only an
    explicit 300–5000 ms value enables it**),
    `ULTRA_COLLAPSE_ESCAPE_ROUNDS` 0→2, `ULTRA_QAM16_CLIMB_STREAK` 2→1,
    `ULTRA_QAM16_RECLIMB_COOLDOWN` 3→1, `ULTRA_R34_CALM_FADING` off→0.30,
    `ULTRA_ALC_MAX_DRIVE` 0.85→0.70 (env may raise toward the 0.85 hard max).
    NOT flipped: `ULTRA_RATE_CLIMB_STREAK` (env-only; RateController default 2 is
    test-pinned and the controller is inert under authority). Wire lockstep: both ends
    must run a flipped build (authority binds the widened tone-ACK CRC span; the CRC32
    table repair changes file-CRC values vs old builds). Launch scripts stripped to
    channel/radio-specific settings only. Test baselines updated the same day
    (waveform/connection policy suites now assert the psk8-exp ladder picks; knob-OFF
    tests pin `=0` explicitly).
10. `ULTRA_WIENER_*`, `ULTRA_REL_FADE_*`, `ULTRA_DD_FADING_MAX` — channel-adaptive params. NOTE
    (2026-05-30): their *defaults* are NOT derived today — `robustDelaySpreadS()`/`robustDopplerHz()`
    return hardcoded Moderate-HF constants (1e-3 s / 0.5 Hz) with no channel input; the comment in
    `channel_equalizer_pilot.cpp:23` admits this is wrong on Good HF (0.5 ms / 0.1 Hz). So "codify"
    here means *replace the constant with a derivation* `f(negotiated coherence time/BW)` — NOT
    freeze the env default into a `constexpr`. This is a proof-gated PHY change, not mechanical
    cleanup; blocked on the estimate work (tasks #8/#9). Until then they stay env-overridable.

**Hot-path getenv (DONE 2026-05-30, behavior-neutral):** `robustDelaySpreadS()`/`robustDopplerHz()`
were called per-carrier inside the Wiener loop (`:243/:287`), and the DD knobs per pilot-interp call
— `getenv()` is a linear `environ` scan and not thread-safe, a real-time hazard. Converted to the
read-once `static` idiom already used in `channel_equalizer_equalize.cpp` (`kRelFadeOnset`). Proven
neutral: `OFDMWienerInterpolator` passes, `cli_simulator` AWGN R1/4 7/7. The remaining ~90 getenv
sites are read at config/handshake time (not hot) — no further hot-path work needed.

---

## 8. Corrections to CLAUDE.md / MEMORY.md (stale facts found by this audit)

These are wrong in the current top-level docs and should be fixed:
- **`docs/KNOWN_BUGS.md` BUG-BURST-STALE-GEOMETRY line refs had DRIFTED** (corrected in place
  2026-07-28): the arm site is `streaming_ofdm_decode.cpp:1536` not 1533 (1533 is
  `descriptor_group_size_locked_ = false`), the descriptor set-site is **878** not 762, and the
  mid-burst gate is **1204** not 1013. The same 762/1013 refs still appear LATER in KNOWN_BUGS
  (BUG-BURST-HEADNULL-DROP era text) and are stale there too — not corrected, they belong to a
  different entry's narrative.
- **The bug entry's ⚠ "OPEN BLOCKER — reconfigure ordering" was NOT a real blocker.**
  `getMinSamplesForCWCount` is closed-form in scalars and its only live-object read
  (`getSamplesPerSymbol()`) is INVARIANT across the profile change being predicted, so a pure
  explicit-params query needs no `configure()` and cannot reach `HilbertTransform::process`.
  The real blocker was different and bigger: the stale call has THREE consumers per group and
  fixing only `burst_min_block_` misaligns the whole group.
- **OFDM_COX is REMOVED from the protocol enum** (`frame_v2.hpp:30` "formerly OFDM_COX —
  removed"), not "forceable/legacy." It is **not selectable**; only the Schmidl-Cox *sync
  primitive* remains. CLAUDE.md (Waveform Summary table, mode notes) and several MEMORY entries
  still call it forced/legacy.
- **ARQ windows:** CLAUDE.md says "window=1 for MC-DPSK" and wideband "up to 8." Actual:
  MC-DPSK is timing-derived **1–5**, OFDM_NARROW is **3**, wideband OFDM is **8 default and up
  to 16** on near-AWGN ≥R1/2, with the unified file/message burst bounded to a ~7 s airtime budget
  per key-down (`prepareUnifiedBurstWindow`). (`BurstStopAndWaitController` was REMOVED 2026-06-06 —
  R1b transport merge; there is no longer a separate window=1 group controller.)
- **pilot_spacing=10 is a default, not a constant** — overwritten per-rung by
  `recommendedPilotSpacing(mod,rate)`.
- **SC-DPSK** is listed in the CLAUDE.md waveform table but is not an `IWaveform` and not in the
  factory.
- **`decode_bench` + `DecodeBenchReplay` RETIRED 2026-05-30** (commit pending): the headless
  WAV-fixture A/B tool + its replay CTest are gone, along with the 6 `fixtures/ofdm_chirp_*dqpsk*.wav`
  fixtures. The replay path had drifted from the live streaming decoder (`frames_decoded=0` on
  *every* fixture — it never entered the 4-CW data path) and all testing is now done on
  `tools/gui_qso_scenario.sh` (live GUI feedback), the trusted floor gate. Consequence: the old
  "OFDM_CHIRP R1/4 Good 15 dB **locked in DecodeBenchReplay**" floor claim is now UNBACKED —
  re-establish that floor on the GUI gate during the ladder rework. (`fixtures/ota_test_r14_15s.wav`,
  a self-contained manual OTA listening fixture, survives.)
- **`cli_simulator` + `test_waveform_simple` + `SimulatedStation` RETIRED 2026-05-30** (commit
  `207a0af`, ~14k lines). They were a divergent TX wrapper around the shared StreamingEncoder/
  Decoder PHY; the GUI's `ModemEngine` path is the production wrapper and `tools/gui_qso_scenario.sh`
  is now THE faithful full-protocol + fade/throughput gate. `measure_ack_fer`/`ota_simulator serve`
  survive (the `ultra_sim_station` lib was un-bundled — it had glued SimulatedStation to the PHY
  sources; `decode_bench` later retired 2026-05-30, see above). Historical `cli_simulator`
  proof-notes above are kept as record.
